"use strict";Object.defineProperty(exports, "__esModule", {value: true}); function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { newObj[key] = obj[key]; } } } newObj.default = obj; return newObj; } } function _nullishCoalesce(lhs, rhsFn) { if (lhs != null) { return lhs; } else { return rhsFn(); } } function _optionalChain(ops) { let lastAccessLHS = undefined; let value = ops[0]; let i = 1; while (i < ops.length) { const op = ops[i]; const fn = ops[i + 1]; i += 2; if ((op === 'optionalAccess' || op === 'optionalCall') && value == null) { return undefined; } if (op === 'access' || op === 'optionalAccess') { lastAccessLHS = value; value = fn(value); } else if (op === 'call' || op === 'optionalCall') { value = fn((...args) => value.call(lastAccessLHS, ...args)); lastAccessLHS = undefined; } } return value; }





var _chunkQGATKC4Rcjs = require('./chunk-QGATKC4R.cjs');
require('./chunk-QETZP25M.cjs');
require('./chunk-KE6DLZAW.cjs');
require('./chunk-EV53YE3W.cjs');


var _chunkE6DONLTMcjs = require('./chunk-E6DONLTM.cjs');
require('./chunk-GS7T56RP.cjs');

// src/ocr/image-ocr.ts
var IMAGE_SCALE = 3;
async function parseImageDocument(buffer, options) {
  const warnings = [];
  if (typeof _optionalChain([options, 'optionalAccess', _ => _.ocr]) === "function") {
    const text = await options.ocr(new Uint8Array(buffer), 1, detectImageMime(buffer));
    if (!text.trim()) {
      warnings.push({ page: 1, message: "OCR \uACB0\uACFC \uC5C6\uC74C", code: "OCR_FAILED" });
      return { blocks: [], warnings };
    }
    return { blocks: [{ type: "paragraph", text: text.trim(), pageNumber: 1 }], warnings };
  }
  const { data, width, height } = await decodeToRgba(buffer);
  await _chunkQGATKC4Rcjs.ensureOcrModels.call(void 0, (p) => {
    if (p.phase === "download" && p.downloaded === 0) {
      process.stderr.write(`[kordoc-ocr] ${p.spec.name} \uB2E4\uC6B4\uB85C\uB4DC \uC911 (~${p.spec.sizeMb}MB)...
`);
    }
  });
  const engine = await _chunkQGATKC4Rcjs.getOcrEngine.call(void 0, );
  const stats = { droppedLowConf: 0 };
  const items = await engine.recognizePage(data, width, height, stats);
  if (stats.droppedLowConf > 0) {
    warnings.push({
      page: 1,
      message: `\uC800\uC2E0\uB8B0 OCR \uB77C\uC778 ${stats.droppedLowConf}\uAC1C \uD3D0\uAE30 (\uC778\uC2DD \uACB0\uC190 \uAC00\uB2A5)`,
      code: "OCR_LOW_CONF"
    });
  }
  if (items.length === 0) {
    warnings.push({ page: 1, message: "\uC774\uBBF8\uC9C0\uC5D0\uC11C \uD14D\uC2A4\uD2B8\uB97C \uC778\uC2DD\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4", code: "OCR_FAILED" });
    return { blocks: [], warnings };
  }
  const pdfW = width / IMAGE_SCALE;
  const pdfH = height / IMAGE_SCALE;
  const ruling = _chunkQGATKC4Rcjs.detectRulingLines.call(void 0, data, width, height, IMAGE_SCALE);
  const extraLines = _chunkQGATKC4Rcjs.rulingToPdfLines.call(void 0, ruling, IMAGE_SCALE, pdfH);
  return { blocks: _chunkQGATKC4Rcjs.ocrItemsToBlocks.call(void 0, items, 1, pdfW, pdfH, IMAGE_SCALE, extraLines, _optionalChain([options, 'optionalAccess', _2 => _2.tables]) !== false), warnings };
}
async function decodeToRgba(buffer) {
  let sharp;
  try {
    const mod = await Promise.resolve().then(() => _interopRequireWildcard(require("sharp")));
    sharp = typeof mod === "function" ? mod : _nullishCoalesce(mod.default, () => ( mod));
  } catch (e) {
    throw new (0, _chunkE6DONLTMcjs.KordocError)(
      `\uC774\uBBF8\uC9C0 \uD30C\uC2F1\uC5D0\uB294 optional dependency 'sharp' \uAC00 \uD544\uC694\uD569\uB2C8\uB2E4. \`npm install sharp\` \uD6C4 \uB2E4\uC2DC \uC2E4\uD589\uD558\uC138\uC694. \uC6D0\uC778: ${e.message}`
    );
  }
  const { data, info } = await sharp(Buffer.from(buffer)).ensureAlpha().raw().toBuffer({ resolveWithObject: true });
  return { data: new Uint8Array(data.buffer, data.byteOffset, data.byteLength), width: info.width, height: info.height };
}
function detectImageMime(buffer) {
  const b = new Uint8Array(buffer, 0, Math.min(12, buffer.byteLength));
  if (b[0] === 255 && b[1] === 216) return "image/jpeg";
  if (b[0] === 82 && b[1] === 73) return "image/webp";
  return "image/png";
}


exports.parseImageDocument = parseImageDocument;
//# sourceMappingURL=image-ocr-75WGMVU6.cjs.map