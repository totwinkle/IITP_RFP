"use strict";Object.defineProperty(exports, "__esModule", {value: true}); function _nullishCoalesce(lhs, rhsFn) { if (lhs != null) { return lhs; } else { return rhsFn(); } } function _optionalChain(ops) { let lastAccessLHS = undefined; let value = ops[0]; let i = 1; while (i < ops.length) { const op = ops[i]; const fn = ops[i + 1]; i += 2; if ((op === 'optionalAccess' || op === 'optionalCall') && value == null) { return undefined; } if (op === 'access' || op === 'optionalAccess') { lastAccessLHS = value; value = fn(value); } else if (op === 'call' || op === 'optionalCall') { value = fn((...args) => value.call(lastAccessLHS, ...args)); lastAccessLHS = undefined; } } return value; }

var _chunkE6DONLTMcjs = require('./chunk-E6DONLTM.cjs');

// src/shared/pua.ts
var BMP_SYMBOL_MAP = {
  // 도형/기호
  108: "\u25CF",
  // ●
  109: "\u25CF",
  // ● (그림자 원 근사)
  110: "\u25A0",
  // ■
  111: "\u25A1",
  // □
  112: "\u25A1",
  // □ (굵은 흰 사각 근사)
  113: "\u25A1",
  // □ (그림자 근사)
  114: "\u25A1",
  // □ (그림자 근사)
  115: "\u2B27",
  // ⬧
  116: "\u29EB",
  // ⧫
  117: "\u25C6",
  // ◆
  118: "\u2756",
  // ❖
  119: "\u2B25",
  // ⬥
  // 체크/별/점
  158: "\xB7",
  // ·
  159: "\u2022",
  // •
  160: "\xB7",
  // · (한컴 PDF 정답지 정합 — ▪ 아님)
  161: "\u26AA",
  // ⚪
  162: "\u25CB",
  // ○
  163: "\u25CB",
  // ○
  164: "\u25C9",
  // ◉
  165: "\u25CE",
  // ◎
  167: "\u25AA",
  // ▪
  168: "\u25FB",
  // ◻
  170: "\u2726",
  // ✦
  171: "\u2605",
  // ★
  172: "\u2736",
  // ✶
  173: "\u2734",
  // ✴
  174: "\u2739",
  // ✹
  // 손 모양
  69: "\u261C",
  // ☜
  70: "\u261E",
  // ☞
  71: "\u261D",
  // ☝
  72: "\u261F",
  // ☟
  // 체크마크
  251: "\u2717",
  // ✗
  252: "\u2714",
  // ✔
  253: "\u2612",
  // ☒
  254: "\u2611",
  // ☑
  // 화살표
  232: "\u2794",
  // ➔ (heavy wide-headed — 한컴 PDF 정답지 정합)
  239: "\u21E6",
  // ⇦
  240: "\u21E8",
  // ⇨
  241: "\u21E7",
  // ⇧
  242: "\u21E9",
  // ⇩
  // 기타
  34: "\u2702",
  // ✂
  54: "\u231B",
  // ⌛
  74: "\u263A",
  // ☺
  78: "\u2620",
  // ☠
  82: "\u263C",
  // ☼
  84: "\u2744",
  // ❄
  88: "\u2720",
  // ✠
  89: "\u2721"
  // ✡
};
var SUPPLEMENTARY_MAP = {
  983099: "\u2193",
  // ↓
  983791: "\xB7",
  // ·
  985172: "\u300A",
  // 《
  985173: "\u300B",
  // 》
  983258: "\u25B8",
  // ▸
  985103: "\u2501",
  // ━
  985127: "\u25A0",
  // ■
  984005: "\u25A1",
  // □ 글머리 — HWP3→HWP5 한컴 변환본 보존 코드, 한컴오피스 표시값 (rhwp #1105)
  // 아래 5종 — rhwp 44cabad9 verified_hancom_pua 표 (한컴 PDF 대조 확정)
  983339: "(\uC778)",
  // 결재·서명란
  983804: "\u25BA",
  // 2025 행정업무운영 편람 callout 글머리
  983836: "\u25A0",
  // 2025 행정업무운영 편람 목차 글머리
  983968: "\u21B5",
  // 하이퍼텍스트 안내문의 Enter 키 픽토그램
  // 머리말 회사명 6자 (HWP3 johab 0x37C0~0x37C5의 HWP5/HWPX 변환본 대응 코드)
  984047: "\uD55C",
  984048: "\uAE00",
  984049: "\uACFC",
  984050: "\uCEF4",
  984051: "\uD4E8",
  984052: "\uD130"
};
var BOXED_NUMBER_START = 983729;
var BOXED_NUMBER_END = 983748;
function mapPuaChar(code) {
  if (code >= 61472 && code <= 61695) {
    return BMP_SYMBOL_MAP[code - 61440];
  }
  if (code >= BOXED_NUMBER_START && code <= BOXED_NUMBER_END) {
    return String.fromCodePoint(9312 + (code - BOXED_NUMBER_START));
  }
  if (code >= 983040 && code <= 985599) {
    return SUPPLEMENTARY_MAP[code];
  }
  return void 0;
}
function mapPuaText(text) {
  let out = "";
  for (const ch of text) {
    const code = ch.codePointAt(0);
    out += _nullishCoalesce(mapPuaChar(code), () => ( ch));
  }
  return out;
}

// src/table/builder.ts
var MAX_COLS = 200;
var MAX_ROWS = 1e4;
function buildTable(rows, options) {
  if (rows.length > MAX_ROWS) rows = rows.slice(0, MAX_ROWS);
  const numRows = rows.length;
  const hasAddr = rows.some((row) => row.some((c) => c.colAddr !== void 0 && c.rowAddr !== void 0));
  if (hasAddr) return buildTableDirect(rows, numRows, options);
  let maxCols = 0;
  const tempOccupied = Array.from({ length: numRows }, () => []);
  for (let rowIdx = 0; rowIdx < numRows; rowIdx++) {
    let colIdx = 0;
    for (const cell of rows[rowIdx]) {
      while (colIdx < MAX_COLS && tempOccupied[rowIdx][colIdx]) colIdx++;
      if (colIdx >= MAX_COLS) break;
      for (let r = rowIdx; r < Math.min(rowIdx + cell.rowSpan, numRows); r++) {
        for (let c = colIdx; c < Math.min(colIdx + cell.colSpan, MAX_COLS); c++) {
          tempOccupied[r][c] = true;
        }
      }
      colIdx += cell.colSpan;
      if (colIdx > maxCols) maxCols = colIdx;
    }
  }
  if (maxCols === 0) return { rows: 0, cols: 0, cells: [], hasHeader: false };
  const grid = Array.from(
    { length: numRows },
    () => Array.from({ length: maxCols }, () => ({ text: "", colSpan: 1, rowSpan: 1 }))
  );
  const occupied = Array.from({ length: numRows }, () => Array(maxCols).fill(false));
  const anchorCols = /* @__PURE__ */ new Set();
  for (let rowIdx = 0; rowIdx < numRows; rowIdx++) {
    let colIdx = 0;
    let cellIdx = 0;
    while (colIdx < maxCols && cellIdx < rows[rowIdx].length) {
      while (colIdx < maxCols && occupied[rowIdx][colIdx]) colIdx++;
      if (colIdx >= maxCols) break;
      const cell = rows[rowIdx][cellIdx];
      anchorCols.add(colIdx);
      grid[rowIdx][colIdx] = {
        text: _optionalChain([options, 'optionalAccess', _2 => _2.keepEmptyParagraphs]) ? cell.text : cell.text.trim(),
        colSpan: cell.colSpan,
        rowSpan: cell.rowSpan
      };
      for (let r = rowIdx; r < Math.min(rowIdx + cell.rowSpan, numRows); r++) {
        for (let c = colIdx; c < Math.min(colIdx + cell.colSpan, maxCols); c++) {
          occupied[r][c] = true;
        }
      }
      colIdx += cell.colSpan;
      cellIdx++;
    }
  }
  return trimAndReturn(grid, numRows, maxCols, anchorCols, options);
}
function buildTableDirect(rows, numRows, options) {
  let maxCols = 0;
  for (const row of rows) {
    for (const cell of row) {
      const end = (_nullishCoalesce(cell.colAddr, () => ( 0))) + cell.colSpan;
      if (end > maxCols) maxCols = end;
    }
  }
  if (maxCols > MAX_COLS) maxCols = MAX_COLS;
  if (maxCols === 0) return { rows: 0, cols: 0, cells: [], hasHeader: false };
  const grid = Array.from(
    { length: numRows },
    () => Array.from({ length: maxCols }, () => ({ text: "", colSpan: 1, rowSpan: 1 }))
  );
  const anchorCols = /* @__PURE__ */ new Set();
  for (const row of rows) {
    for (const cell of row) {
      const r = _nullishCoalesce(cell.rowAddr, () => ( 0));
      const c = _nullishCoalesce(cell.colAddr, () => ( 0));
      if (r >= numRows || c >= maxCols || r < 0 || c < 0) continue;
      anchorCols.add(c);
      grid[r][c] = { text: _optionalChain([options, 'optionalAccess', _3 => _3.keepEmptyParagraphs]) ? cell.text : cell.text.trim(), colSpan: cell.colSpan, rowSpan: cell.rowSpan };
      for (let dr = 0; dr < cell.rowSpan; dr++) {
        for (let dc = 0; dc < cell.colSpan; dc++) {
          if (dr === 0 && dc === 0) continue;
          if (r + dr < numRows && c + dc < maxCols) {
            grid[r + dr][c + dc] = { text: "", colSpan: 1, rowSpan: 1 };
          }
        }
      }
    }
  }
  return trimAndReturn(grid, numRows, maxCols, anchorCols, options);
}
function trimAndReturn(grid, numRows, maxCols, anchorCols, options) {
  let effectiveCols = maxCols;
  while (effectiveCols > 0) {
    const colEmpty = grid.every((row) => !_optionalChain([row, 'access', _4 => _4[effectiveCols - 1], 'optionalAccess', _5 => _5.text, 'optionalAccess', _6 => _6.trim, 'call', _7 => _7()]));
    if (!colEmpty) break;
    if (_optionalChain([options, 'optionalAccess', _8 => _8.keepAnchoredEmptyCols]) && anchorCols.has(effectiveCols - 1)) break;
    effectiveCols--;
  }
  if (effectiveCols < maxCols && effectiveCols > 0) {
    const trimmed = grid.map((row) => row.slice(0, effectiveCols));
    return { rows: numRows, cols: effectiveCols, cells: trimmed, hasHeader: numRows > 1 };
  }
  return { rows: numRows, cols: maxCols, cells: grid, hasHeader: numRows > 1 };
}
function convertTableToText(rows) {
  return rows.map(
    (row) => row.map((c) => c.text.trim().replace(/\n/g, " ").replace(/\|/g, "\\|")).filter(Boolean).join(" / ")
  ).filter(Boolean).join("\n");
}
function escapeGfm(text) {
  const NUL = String.fromCharCode(0);
  const spans = [];
  const masked = text.replace(/!\[[^\]]*\]\([^)\n]*\)|\]\((?:https?:|mailto:|tel:|#)[^)\n]*\)|\$\$[^$]*\$\$|\$[^$\n]*\$/gi, (m) => {
    spans.push(m);
    return NUL + (spans.length - 1) + NUL;
  });
  const escaped = masked.replace(/([~*_`])/g, "\\$1");
  return escaped.replace(new RegExp(NUL + "(\\d+)" + NUL, "g"), (_, n) => spans[Number(n)]);
}
var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/gm;
function sanitizeText(text) {
  let result = mapPuaText(text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
  if (result.length <= 30 && result.includes(" ")) {
    const tokens = result.split(" ");
    const koreanSingleCharCount = tokens.filter((t) => t.length === 1 && /[\uAC00-\uD7AF\u3131-\u318E]/.test(t)).length;
    if (tokens.length >= 3 && koreanSingleCharCount / tokens.length >= 0.7) {
      result = tokens.join("");
    }
  }
  return result;
}
function flattenLayoutTables(blocks) {
  const result = [];
  for (const block of blocks) {
    if (block.type !== "table" || !block.table) {
      result.push(block);
      continue;
    }
    const { rows: numRows, cols: numCols, cells } = block.table;
    if (numRows === 1 && numCols === 1) {
      result.push(block);
      continue;
    }
    if (numRows <= 3) {
      let totalNewlines = 0;
      let totalTextLen = 0;
      for (let r = 0; r < numRows; r++) {
        for (let c = 0; c < numCols; c++) {
          const t = _optionalChain([cells, 'access', _9 => _9[r], 'optionalAccess', _10 => _10[c], 'optionalAccess', _11 => _11.text]) || "";
          totalNewlines += (t.match(/\n/g) || []).length;
          totalTextLen += t.length;
        }
      }
      if (numCols < 4 && (totalNewlines > 5 || numRows <= 2 && totalTextLen > 300)) {
        for (let r = 0; r < numRows; r++) {
          for (let c = 0; c < numCols; c++) {
            const cell = _optionalChain([cells, 'access', _12 => _12[r], 'optionalAccess', _13 => _13[c]]);
            if (!cell) continue;
            if (_optionalChain([cell, 'access', _14 => _14.blocks, 'optionalAccess', _15 => _15.length])) {
              result.push(...flattenLayoutTables(cell.blocks));
              continue;
            }
            const cellText = _optionalChain([cell, 'access', _16 => _16.text, 'optionalAccess', _17 => _17.trim, 'call', _18 => _18()]);
            if (!cellText) continue;
            for (const line of cellText.split("\n")) {
              const trimmed = line.trim();
              if (!trimmed) continue;
              result.push({ type: "paragraph", text: trimmed, pageNumber: block.pageNumber });
            }
          }
        }
        continue;
      }
    }
    result.push(block);
  }
  return result;
}
var RUNNING_HEADER_MAX_LEN = 40;
var RUNNING_HEADER_MIN_FREQ = 3;
function isRunningHeaderCandidate(block) {
  if (block.type !== "paragraph" && block.type !== "heading") return false;
  const text = _optionalChain([block, 'access', _19 => _19.text, 'optionalAccess', _20 => _20.trim, 'call', _21 => _21()]);
  if (!text || text.length > RUNNING_HEADER_MAX_LEN) return false;
  return /^\d+\.\s/.test(text);
}
function dedupeRunningHeaders(blocks) {
  const freq = /* @__PURE__ */ new Map();
  for (const block of blocks) {
    if (!isRunningHeaderCandidate(block)) continue;
    const text = block.text.trim();
    freq.set(text, (_nullishCoalesce(freq.get(text), () => ( 0))) + 1);
  }
  const seen = /* @__PURE__ */ new Set();
  const result = [];
  for (const block of blocks) {
    if (isRunningHeaderCandidate(block)) {
      const text = block.text.trim();
      if ((_nullishCoalesce(freq.get(text), () => ( 0))) >= RUNNING_HEADER_MIN_FREQ) {
        if (seen.has(text)) continue;
        seen.add(text);
      }
    }
    result.push(block);
  }
  return result;
}
function spansToMarkdown(spans) {
  let out = "";
  for (const s of spans) {
    if (!s.text) continue;
    let marker = s.code ? "`" : s.bold && s.italic ? "***" : s.bold ? "**" : s.italic ? "*" : "";
    if (s.strike && !s.code) marker = `~~${marker}`;
    const uWrap = !!s.underline && !s.code;
    if (!marker && !uWrap) {
      out += escapeGfm(s.text);
      continue;
    }
    const m = /^(\s*)([\s\S]*?)(\s*)$/.exec(s.text);
    const core = m[2];
    if (!core) {
      out += s.text;
      continue;
    }
    let open = marker;
    let close = [...marker].reverse().join("");
    if (uWrap) {
      open = "<u>" + open;
      close = close + "</u>";
    }
    out += m[1] + open + (s.code ? core : escapeGfm(core)) + close + m[3];
  }
  return out;
}
function blocksToMarkdown(blocks) {
  const lines = [];
  for (let i = 0; i < blocks.length; i++) {
    const block = blocks[i];
    if (block.type === "heading" && block.text) {
      const prefix = "#".repeat(Math.min(block.level || 2, 6));
      const headingText = sanitizeText(block.text);
      if (headingText) lines.push("", `${prefix} ${escapeGfm(headingText)}`, "");
      continue;
    }
    if (block.type === "image" && block.text) {
      lines.push("", `![image](${block.text})`, "");
      continue;
    }
    if (block.type === "separator") {
      lines.push("", "---", "");
      continue;
    }
    if (block.type === "list" && block.text) {
      const listText = sanitizeText(block.text);
      if (!listText) continue;
      const alreadyNumbered = block.listType === "ordered" && /^\d+\.\s/.test(listText);
      const prefix = alreadyNumbered ? "" : block.listType === "ordered" ? "1. " : "- ";
      lines.push(`${prefix}${escapeGfm(listText)}`);
      if (block.children) {
        for (const child of block.children) {
          const childPrefix = child.listType === "ordered" ? "1." : "-";
          lines.push(`  ${childPrefix} ${escapeGfm(child.text || "")}`);
        }
      }
      continue;
    }
    if (block.type === "paragraph" && block.text) {
      let text = sanitizeText(block.text);
      if (!text) continue;
      if (/^\[별표\s*\d+/.test(text)) {
        const nextBlock = blocks[i + 1];
        if (_optionalChain([nextBlock, 'optionalAccess', _22 => _22.type]) === "paragraph" && nextBlock.text && /관련\)?$/.test(nextBlock.text)) {
          lines.push("", `## ${escapeGfm(text)} ${escapeGfm(nextBlock.text)}`, "");
          i++;
        } else {
          lines.push("", `## ${escapeGfm(text)}`, "");
        }
        continue;
      }
      if (/^\([^)]*조[^)]*관련\)$/.test(text)) {
        lines.push(`*${escapeGfm(text)}*`, "");
        continue;
      }
      const listIndent = block.listDepth ? "  ".repeat(block.listDepth) : "";
      if (_optionalChain([block, 'access', _23 => _23.spans, 'optionalAccess', _24 => _24.length])) {
        let rendered = spansToMarkdown(block.spans);
        if (block.href) {
          const href = _chunkE6DONLTMcjs.sanitizeHref.call(void 0, block.href);
          if (href) rendered = `[${rendered}](${href})`;
        }
        if (block.footnoteText) rendered += ` (\uC8FC: ${block.footnoteText})`;
        lines.push(block.quote ? "> " + rendered : listIndent + rendered, "");
        continue;
      }
      if (block.href) {
        const href = _chunkE6DONLTMcjs.sanitizeHref.call(void 0, block.href);
        if (href) text = `[${text}](${href})`;
      }
      if (block.footnoteText) {
        text += ` (\uC8FC: ${block.footnoteText})`;
      }
      lines.push(block.quote ? "> " + escapeGfm(text) : listIndent + escapeGfm(text), "");
    } else if (block.type === "table" && block.table) {
      if (lines.length > 0 && lines[lines.length - 1] !== "") {
        lines.push("");
      }
      if (block.table.caption) {
        const caption = sanitizeText(block.table.caption);
        if (caption) lines.push(`**${escapeGfm(caption)}**`, "");
      }
      const tableMd = tableToMarkdown(block.table);
      if (tableMd) {
        lines.push(tableMd);
        lines.push("");
      }
    }
  }
  return lines.join("\n").trim();
}
function hasMergedCells(table) {
  for (const row of table.cells) {
    for (const cell of row) {
      if (cell.colSpan > 1 || cell.rowSpan > 1) return true;
    }
  }
  return false;
}
function hasNestedTables(table) {
  for (const row of table.cells) {
    for (const cell of row) {
      if (_optionalChain([cell, 'access', _25 => _25.blocks, 'optionalAccess', _26 => _26.some, 'call', _27 => _27((b) => b.type === "table" && b.table)])) return true;
    }
  }
  return false;
}
function cellInnerHtml(cell) {
  if (_optionalChain([cell, 'access', _28 => _28.blocks, 'optionalAccess', _29 => _29.length])) {
    return cell.blocks.map((b) => {
      if (b.type === "table" && b.table) {
        const cap = b.table.caption ? sanitizeText(b.table.caption) : "";
        return (cap ? cap + "<br>" : "") + tableToHtml(b.table);
      }
      if (b.type === "image" && b.text) return `<img src="${b.text}" alt="image">`;
      const t = sanitizeText(_nullishCoalesce(b.text, () => ( "")));
      return t ? t.replace(/\n/g, "<br>") : "";
    }).filter(Boolean).join("<br>");
  }
  return sanitizeText(cell.text).replace(/\n/g, "<br>");
}
function containsInlineMath(text) {
  return /(^|[^\\])\$(?=\S)(?:[^$\n\\]|\\.)+?\S\$/.test(text);
}
function tableContainsInlineMath(table) {
  for (const row of table.cells) {
    for (const cell of row) {
      if (containsInlineMath(cell.text)) return true;
    }
  }
  return false;
}
function tableToHtml(table) {
  const { cells, rows: numRows, cols: numCols } = table;
  const skip = /* @__PURE__ */ new Set();
  const lines = ["<table>"];
  for (let r = 0; r < numRows; r++) {
    const tag = r === 0 ? "th" : "td";
    const rowHtml = [];
    for (let c = 0; c < numCols; c++) {
      if (skip.has(`${r},${c}`)) continue;
      const cell = _optionalChain([cells, 'access', _30 => _30[r], 'optionalAccess', _31 => _31[c]]);
      if (!cell) continue;
      for (let dr = 0; dr < cell.rowSpan; dr++) {
        for (let dc = 0; dc < cell.colSpan; dc++) {
          if (dr === 0 && dc === 0) continue;
          if (r + dr < numRows && c + dc < numCols) skip.add(`${r + dr},${c + dc}`);
        }
      }
      const text = cellInnerHtml(cell);
      const attrs = [];
      if (cell.colSpan > 1) attrs.push(`colspan="${cell.colSpan}"`);
      if (cell.rowSpan > 1) attrs.push(`rowspan="${cell.rowSpan}"`);
      const attrStr = attrs.length ? " " + attrs.join(" ") : "";
      rowHtml.push(`<${tag}${attrStr}>${text}</${tag}>`);
    }
    if (rowHtml.length) lines.push(`<tr>${rowHtml.join("")}</tr>`);
  }
  lines.push("</table>");
  return lines.join("\n");
}
function tableToMarkdown(table) {
  if (table.rows === 0 || table.cols === 0) return "";
  const { cells, rows: numRows, cols: numCols } = table;
  if ((hasMergedCells(table) || hasNestedTables(table)) && !tableContainsInlineMath(table)) {
    return tableToHtml(table);
  }
  if (numRows === 1 && numCols === 1) {
    const content = sanitizeText(cells[0][0].text);
    if (!content) return "";
    return content.split(/\n/).map((line) => {
      const trimmed = line.trim();
      if (!trimmed) return "";
      if (/^\d+\.\s/.test(trimmed)) return `**${escapeGfm(trimmed)}**`;
      if (/^[가-힣]\.\s/.test(trimmed)) return `  ${escapeGfm(trimmed)}`;
      return escapeGfm(trimmed);
    }).filter(Boolean).join("\n");
  }
  if (numCols === 1 && numRows >= 2) {
    return cells.map((row) => escapeGfm(sanitizeText(row[0].text)).replace(/\n/g, " ")).filter(Boolean).join("\n");
  }
  const display = Array.from({ length: numRows }, () => Array(numCols).fill(""));
  const skip = /* @__PURE__ */ new Set();
  for (let r = 0; r < numRows; r++) {
    for (let c = 0; c < numCols; c++) {
      if (skip.has(`${r},${c}`)) continue;
      const cell = _optionalChain([cells, 'access', _32 => _32[r], 'optionalAccess', _33 => _33[c]]);
      if (!cell) continue;
      display[r][c] = (_optionalChain([cell, 'access', _34 => _34.blocks, 'optionalAccess', _35 => _35.some, 'call', _36 => _36((b) => b.spans)]) ? cell.blocks.map((b) => b.type === "image" && b.text ? `![image](${b.text})` : b.spans ? spansToMarkdown(b.spans) : escapeGfm(sanitizeText(_nullishCoalesce(b.text, () => ( ""))))).filter(Boolean).join("<br>") : escapeGfm(sanitizeText(cell.text)).replace(/\n/g, "<br>")).replace(/\|/g, "\\|");
      for (let dr = 0; dr < cell.rowSpan; dr++) {
        for (let dc = 0; dc < cell.colSpan; dc++) {
          if (dr === 0 && dc === 0) continue;
          if (r + dr < numRows && c + dc < numCols) {
            skip.add(`${r + dr},${c + dc}`);
          }
        }
      }
      c += cell.colSpan - 1;
    }
  }
  const uniqueRows = [];
  let pendingLabelRow = null;
  for (let r = 0; r < display.length; r++) {
    const row = display[r];
    if (row.every((cell) => cell === "")) {
      if (row.some((_, c) => skip.has(`${r},${c}`))) continue;
      if (pendingLabelRow) {
        uniqueRows.push(pendingLabelRow);
        pendingLabelRow = null;
      }
      uniqueRows.push(row);
      continue;
    }
    const nonEmptyCols = row.filter((cell) => cell !== "");
    const hasSkipInRow = row.some((_, c) => skip.has(`${r},${c}`));
    if (!hasSkipInRow && nonEmptyCols.length === 1 && row[0] !== "" && row.slice(1).every((c) => c === "")) {
      if (pendingLabelRow) uniqueRows.push(pendingLabelRow);
      pendingLabelRow = row;
      continue;
    }
    if (pendingLabelRow) {
      if (row[0] === "") row[0] = pendingLabelRow[0];
      else uniqueRows.push(pendingLabelRow);
      pendingLabelRow = null;
    }
    uniqueRows.push(row);
  }
  if (pendingLabelRow) uniqueRows.push(pendingLabelRow);
  if (uniqueRows.length === 0) return "";
  const md = [];
  md.push("| " + uniqueRows[0].join(" | ") + " |");
  md.push("| " + uniqueRows[0].map(() => "---").join(" | ") + " |");
  for (let i = 1; i < uniqueRows.length; i++) {
    md.push("| " + uniqueRows[i].join(" | ") + " |");
  }
  return md.join("\n");
}

// src/image/transcode.ts
var _zlib = require('zlib');
var CRC_TABLE = (() => {
  const table = new Uint32Array(256);
  for (let n = 0; n < 256; n++) {
    let c = n;
    for (let k = 0; k < 8; k++) {
      c = c & 1 ? 3988292384 ^ c >>> 1 : c >>> 1;
    }
    table[n] = c >>> 0;
  }
  return table;
})();
function crc32(bytes) {
  let c = 4294967295;
  for (let i = 0; i < bytes.length; i++) {
    c = CRC_TABLE[(c ^ bytes[i]) & 255] ^ c >>> 8;
  }
  return (c ^ 4294967295) >>> 0;
}
var BI_RGB = 0;
var MAX_DIM = 32767;
var MAX_PIXELS = 36e6;
function bmpToPng(bmp) {
  if (bmp.length < 54) return null;
  if (bmp[0] !== 66 || bmp[1] !== 77) return null;
  const dv = new DataView(bmp.buffer, bmp.byteOffset, bmp.byteLength);
  const dataOffset = dv.getUint32(10, true);
  const headerSize = dv.getUint32(14, true);
  if (headerSize < 40) return null;
  const width = dv.getInt32(18, true);
  const rawHeight = dv.getInt32(22, true);
  const bitCount = dv.getUint16(28, true);
  const compression = dv.getUint32(30, true);
  if (compression !== BI_RGB) return null;
  if (bitCount !== 24 && bitCount !== 32) return null;
  if (width <= 0 || rawHeight === 0) return null;
  if (width > MAX_DIM || Math.abs(rawHeight) > MAX_DIM) return null;
  const topDown = rawHeight < 0;
  const height = Math.abs(rawHeight);
  if (width * height > MAX_PIXELS) return null;
  const bytesPerPixel = bitCount >> 3;
  const rowStride = width * bytesPerPixel + 3 & ~3;
  if (dataOffset + rowStride * height > bmp.length) return null;
  const rgba = new Uint8Array(width * height * 4);
  let anyAlpha = 0;
  for (let y = 0; y < height; y++) {
    const srcRow = topDown ? y : height - 1 - y;
    let src = dataOffset + srcRow * rowStride;
    let dst = y * width * 4;
    for (let x = 0; x < width; x++) {
      rgba[dst] = bmp[src + 2];
      rgba[dst + 1] = bmp[src + 1];
      rgba[dst + 2] = bmp[src];
      const a = bitCount === 32 ? bmp[src + 3] : 255;
      rgba[dst + 3] = a;
      anyAlpha |= a;
      src += bytesPerPixel;
      dst += 4;
    }
  }
  if (bitCount === 32 && anyAlpha === 0) {
    for (let i = 3; i < rgba.length; i += 4) rgba[i] = 255;
  }
  return encodePng(width, height, rgba);
}
var PNG_SIGNATURE = Uint8Array.of(137, 80, 78, 71, 13, 10, 26, 10);
function chunk(type, data) {
  const body = new Uint8Array(4 + data.length);
  body[0] = type.charCodeAt(0);
  body[1] = type.charCodeAt(1);
  body[2] = type.charCodeAt(2);
  body[3] = type.charCodeAt(3);
  body.set(data, 4);
  const out = new Uint8Array(8 + data.length + 4);
  const dv = new DataView(out.buffer);
  dv.setUint32(0, data.length, false);
  out.set(body, 4);
  dv.setUint32(8 + data.length, crc32(body), false);
  return out;
}
function encodePng(width, height, rgba) {
  const ihdr = new Uint8Array(13);
  const iv = new DataView(ihdr.buffer);
  iv.setUint32(0, width, false);
  iv.setUint32(4, height, false);
  ihdr[8] = 8;
  ihdr[9] = 6;
  const stride = width * 4;
  const raw = new Uint8Array((stride + 1) * height);
  for (let y = 0; y < height; y++) {
    const rowStart = y * (stride + 1);
    raw[rowStart] = 0;
    raw.set(rgba.subarray(y * stride, y * stride + stride), rowStart + 1);
  }
  const idat = _zlib.deflateSync.call(void 0, raw);
  const ihdrChunk = chunk("IHDR", ihdr);
  const idatChunk = chunk("IDAT", idat);
  const iendChunk = chunk("IEND", new Uint8Array(0));
  const out = new Uint8Array(PNG_SIGNATURE.length + ihdrChunk.length + idatChunk.length + iendChunk.length);
  let o = 0;
  out.set(PNG_SIGNATURE, o);
  o += PNG_SIGNATURE.length;
  out.set(ihdrChunk, o);
  o += ihdrChunk.length;
  out.set(idatChunk, o);
  o += idatChunk.length;
  out.set(iendChunk, o);
  return out;
}
function inlineImagesIntoMarkdown(markdown, images, opts) {
  const compress = _optionalChain([opts, 'optionalAccess', _37 => _37.compress]) !== false;
  const uris = /* @__PURE__ */ new Map();
  for (const img of images) {
    let bytes = img.data;
    let mime = img.mimeType;
    if (compress && mime === "image/bmp") {
      const png = bmpToPng(img.data);
      if (png) {
        bytes = png;
        mime = "image/png";
      }
    }
    uris.set(img.filename, `data:${mime};base64,${Buffer.from(bytes).toString("base64")}`);
  }
  if (uris.size === 0) return markdown;
  let out = markdown.replace(/!\[image\]\((?:images\/)?([^()\s]+)\)/g, (m, name) => {
    const uri = uris.get(name);
    return uri !== void 0 ? `![image](${uri})` : m;
  });
  out = out.replace(/(<img\b[^>]*\bsrc=")(?:images\/)?([^"]*)(")/g, (m, pre, name, post) => {
    const uri = uris.get(name);
    return uri !== void 0 ? `${pre}${uri}${post}` : m;
  });
  return out;
}












exports.mapPuaText = mapPuaText; exports.MAX_COLS = MAX_COLS; exports.MAX_ROWS = MAX_ROWS; exports.buildTable = buildTable; exports.convertTableToText = convertTableToText; exports.flattenLayoutTables = flattenLayoutTables; exports.dedupeRunningHeaders = dedupeRunningHeaders; exports.blocksToMarkdown = blocksToMarkdown; exports.encodePng = encodePng; exports.inlineImagesIntoMarkdown = inlineImagesIntoMarkdown;
//# sourceMappingURL=chunk-R4LXZRYP.cjs.map