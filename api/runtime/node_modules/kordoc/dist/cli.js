#!/usr/bin/env node
import {
  parseFormatProfileJson
} from "./chunk-3P5YCJQE.js";
import {
  BODY_FONTS,
  BULLET2_CHARS,
  H2_MARKERS,
  buildGongmunOptions
} from "./chunk-5RPYBC2Q.js";
import {
  BUILTIN_TEMPLATES,
  PRESET_ALIAS,
  detectFormat,
  detectZipFormat,
  extractClickHereFields,
  extractFormFields,
  fillFormFields,
  fillHwpx,
  fillWithUniqueGuard,
  gongmunLintWarnings,
  hwpxToProfile,
  incompatibleGongmunWarnings,
  lintGongmunText,
  markdownToHwpx,
  parse,
  readBuiltinTemplate,
  resolveBuiltinTemplate,
  unknownFontWarnings
} from "./chunk-W5NZYPRQ.js";
import "./chunk-7FCNOKOR.js";
import "./chunk-4KGGVCYX.js";
import "./chunk-MOL7MDBG.js";
import "./chunk-NYLYIN55.js";
import "./chunk-DZIXKL7E.js";
import "./chunk-CGJDCZA3.js";
import {
  blocksToMarkdown
} from "./chunk-KEGAEHWO.js";
import "./chunk-XOO6YEZE.js";
import "./chunk-VZ3AQVBN.js";
import {
  VERSION,
  classifyError,
  sanitizeError,
  toArrayBuffer
} from "./chunk-IMFIACHP.js";

// src/cli.ts
import { readFileSync, writeFileSync, mkdirSync, statSync } from "fs";
import { basename, dirname, resolve, extname } from "path";
import { Command } from "commander";
var program = new Command();
program.name("kordoc").description("\uBAA8\uB450 \uD30C\uC2F1\uD574\uBC84\uB9AC\uACA0\uB2E4 \u2014 HWP, HWPX, PDF, XLSX, DOCX, \uC774\uBBF8\uC9C0(PNG/JPG/WebP) \u2192 Markdown").version(VERSION).argument("<files...>", "\uBCC0\uD658\uD560 \uD30C\uC77C \uACBD\uB85C (HWP, HWPX, PDF, XLSX, DOCX, PNG/JPG/WebP \u2014 \uC774\uBBF8\uC9C0\uB294 OCR \uC790\uB3D9 \uC801\uC6A9)").option("-o, --output <path>", "\uCD9C\uB825 \uD30C\uC77C \uACBD\uB85C (\uB2E8\uC77C \uD30C\uC77C \uC2DC)").option("-d, --out-dir <dir>", "\uCD9C\uB825 \uB514\uB809\uD1A0\uB9AC (\uB2E4\uC911 \uD30C\uC77C \uC2DC)").option("-p, --pages <range>", "\uD398\uC774\uC9C0 \uBC94\uC704 (\uC608: 1-3, 1,3,5) \u2014 \uD55C\uCEF4 \uC800\uC7A5\uBCF8\uC740 \uC2E4\uC81C \uD398\uC774\uC9C0, \uC870\uD310 \uCE90\uC2DC \uC5C6\uC73C\uBA74 \uC139\uC158 \uADFC\uC0AC").option("--format <type>", "\uCD9C\uB825 \uD615\uC2DD: markdown (\uAE30\uBCF8), json, chunks (RAG\uC6A9 \uAD6C\uC870 \uCCAD\uD06C JSON \u2014 \uD5E4\uB529\xB7\uAC1C\uC870\uC2DD \uC704\uACC4 breadcrumb + \uD45C \uB3C5\uB9BD \uCCAD\uD06C)", "markdown").option("--no-header-footer", "PDF \uBA38\uB9AC\uAE00/\uBC14\uB2E5\uAE00 \uC790\uB3D9 \uC81C\uAC70 \uB044\uAE30 (\uAE30\uBCF8: \uC81C\uAC70\uD568)").option("--no-tables", "PDF \uD45C \uAC10\uC9C0 \uB044\uAE30 \u2014 \uD14C\uB450\uB9AC \uBC15\uC2A4\uB97C \uD45C\uB85C \uC624\uC778\uD574 \uC77D\uAE30 \uC21C\uC11C\uAC00 \uB4A4\uC9D1\uD788\uB294 \uBB38\uC11C(2\uB2E8 \uC2DC\uD5D8\uC9C0 \uB4F1)\uC5D0\uC11C \uC790\uC5F0 \uC77D\uAE30\uC21C \uD14D\uC2A4\uD2B8\uB9CC \uBF51\uB294\uB2E4 (#64)").option("--formula-ocr", "PDF \uC218\uC2DD OCR \uD65C\uC131\uD654 (MFD+MFR ONNX, \uCCAB \uC0AC\uC6A9 \uC2DC \uBAA8\uB378 ~155MB \uC790\uB3D9 \uB2E4\uC6B4\uB85C\uB4DC)").option("--ocr", "\uC2A4\uCE94/\uC774\uBBF8\uC9C0 PDF \uD14D\uC2A4\uD2B8 OCR (\uB0B4\uC7A5 PP-OCRv5 korean, \uCCAB \uC0AC\uC6A9 \uC2DC \uBAA8\uB378 ~18MB \uC790\uB3D9 \uB2E4\uC6B4\uB85C\uB4DC \u2014 OCR \uD544\uC694 \uD398\uC774\uC9C0\uB9CC \uC778\uC2DD)").option("--ocr-force", "\uC804 \uD398\uC774\uC9C0 \uAC15\uC81C OCR (\uD14D\uC2A4\uD2B8\uCE35\uC774 \uC788\uC5B4\uB3C4 \uBB34\uC2DC\uD558\uACE0 \uC7AC\uC778\uC2DD)").option("--dedupe-headers", "HWP5 \uB808\uC774\uC544\uC6C3 \uD45C \uD398\uC774\uC9C0 \uBC18\uBCF5 \uB7EC\uB2DD \uD5E4\uB354 \uC911\uBCF5 \uC81C\uAC70 (\uAE30\uBCF8 off \u2014 \uBD99\uC784\uBCC4 \uC7AC\uBC88\uD638 \uC624\uC0AD\uC81C \uC8FC\uC758)").option("--keep-empty-cols", "\uD45C \uC624\uB978\uCABD \uB05D \uBE48 \uC5F4(\uC11C\uC2DD \uC785\uB825\uB780) \uBCF4\uC874 (#47, \uAE30\uBCF8 off: \uD6C4\uD589 \uBE48 \uC5F4 \uD2B8\uB9BC)").option("--keep-empty-paragraphs", "\uBE48 \uBB38\uB2E8 \uBCF4\uC874 \u2014 \uBCF8\uBB38\uC740 \uBE48 paragraph \uBE14\uB85D, \uD45C \uC140\uC740 \uBE48 \uC904\uB85C (#57, \uAE30\uBCF8 off: \uBE48 \uBB38\uB2E8 \uC81C\uAC70)").option("--inline-images", "\uC774\uBBF8\uC9C0\uB97C base64 data URI \uB85C \uB9C8\uD06C\uB2E4\uC6B4\uC5D0 \uC778\uB77C\uC778 (BMP\u2192PNG \uC555\uCD95, HWP5 \uC804\uC6A9 \u2014 \uC778\uB77C\uC778\uB41C \uACBD\uC6B0\uB9CC \uD30C\uC77C \uBBF8\uC800\uC7A5, \uADF8 \uC678 \uD3EC\uB9F7\uC740 \uC800\uC7A5 \uC720\uC9C0)").option("--image-refs", "--format json \uC5D0\uC11C \uC774\uBBF8\uC9C0 \uBC14\uC774\uD2B8\uB97C \uC778\uB77C\uC778\uD558\uC9C0 \uC54A\uACE0 \uD30C\uC77C \uCC38\uC870(images/<\uD30C\uC77C\uBA85>)\uB9CC \uB0A8\uAE40 (#65 \u2014 \uC774\uBBF8\uC9C0\uAC00 \uC218\uBC31 \uC7A5\uC778 \uBB38\uC11C\uC758 \uC9C1\uB82C\uD654 \uD55C\uACC4 \uD68C\uD53C, -o/-d \uC640 \uD568\uAED8 \uC0AC\uC6A9)").option("--password <pw>", "\uC554\uD638\uB85C \uBCF4\uD638\uB41C \uBB38\uC11C\uC758 \uC5F4\uAE30 \uC554\uD638 (#59, HWPX\xB7HWP3\xB7HWP5. \uD55C\uCEF4 DRM \uBB38\uC11C\uB294 \uD574\uB2F9 \uC5C6\uC74C)").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (files, opts) => {
  const validFormats = ["markdown", "json", "chunks"];
  if (!validFormats.includes(opts.format)) {
    process.stderr.write(`[kordoc] \uC9C0\uC6D0\uD558\uC9C0 \uC54A\uB294 \uD615\uC2DD: ${opts.format} (markdown, json, chunks)
`);
    process.exit(1);
  }
  if (opts.output && files.length > 1) {
    process.stderr.write(`[kordoc] \u26A0\uFE0F -o/--output \uC740 \uB2E8\uC77C \uD30C\uC77C \uC804\uC6A9\uC774\uB77C \uBB34\uC2DC\uB429\uB2C8\uB2E4 \u2014 \uB2E4\uC911 \uD30C\uC77C\uC740 -d/--out-dir \uB97C \uC0AC\uC6A9\uD558\uC138\uC694
`);
  }
  for (let fi = 0; fi < files.length; fi++) {
    const filePath = files[fi];
    const absPath = resolve(filePath);
    const fileName = basename(absPath);
    const filePrefix = files.length > 1 ? `[${fi + 1}/${files.length}] ` : "";
    let detectedFormat = "unknown";
    try {
      const fileSize = statSync(absPath).size;
      if (fileSize > 500 * 1024 * 1024) {
        process.stderr.write(`
[kordoc] SKIP: ${fileName} \u2014 \uD30C\uC77C\uC774 \uB108\uBB34 \uD07D\uB2C8\uB2E4 (${(fileSize / 1024 / 1024).toFixed(1)}MB)
`);
        process.exitCode = 1;
        continue;
      }
      const buffer = readFileSync(absPath);
      const arrayBuffer = toArrayBuffer(buffer);
      const format = detectFormat(arrayBuffer);
      detectedFormat = format;
      if (!opts.silent) {
        process.stderr.write(`[kordoc] ${filePrefix}${fileName} (${format}) ...`);
      }
      const parseOptions = { filePath: absPath };
      if (opts.pages) parseOptions.pages = opts.pages;
      if (opts.headerFooter === false) parseOptions.removeHeaderFooter = false;
      if (opts.tables === false) parseOptions.tables = false;
      if (opts.formulaOcr) parseOptions.formulaOcr = true;
      if (opts.ocrForce) parseOptions.ocr = "force";
      else if (opts.ocr) parseOptions.ocr = true;
      if (opts.dedupeHeaders) parseOptions.dedupeRunningHeaders = true;
      if (opts.keepEmptyCols) parseOptions.keepTrailingEmptyCols = true;
      if (opts.keepEmptyParagraphs) parseOptions.keepEmptyParagraphs = true;
      if (opts.inlineImages) parseOptions.inlineImages = true;
      if (opts.password) parseOptions.password = opts.password;
      if (!opts.silent) {
        parseOptions.onProgress = (current, total) => {
          process.stderr.write(`\r[kordoc] ${filePrefix}${fileName} (${format}) [${current}/${total}]`);
        };
      }
      const result = await parse(arrayBuffer, parseOptions);
      detectedFormat = result.fileType;
      if (!result.success) {
        process.stderr.write(` FAIL
`);
        process.stderr.write(`  \u2192 ${result.error}
`);
        if (opts.format === "json") {
          process.stdout.write(JSON.stringify(result, null, 2) + "\n");
        }
        process.exitCode = 1;
        continue;
      }
      if (!opts.silent) process.stderr.write(` OK
`);
      let markdown = result.markdown;
      const imagesInlined = opts.inlineImages && result.fileType === "hwp";
      if (opts.outDir && result.images?.length && !imagesInlined) {
        markdown = markdown.replace(/!\[image\]\(image_/g, "![image](images/image_").replace(/(<img\b[^>]*\bsrc=")image_/g, "$1images/image_");
      }
      const serializeJson = (refsOnly) => {
        const payload = refsOnly && result.images?.length ? { ...result, images: result.images.map((img) => ({ filename: img.filename, mimeType: img.mimeType, path: `images/${img.filename}` })) } : result;
        return JSON.stringify(
          payload,
          (_key, value) => value instanceof Uint8Array ? Buffer.from(value).toString("base64") : value,
          2
        );
      };
      let output;
      if (opts.format === "json") {
        const savesImages = Boolean(opts.output && files.length === 1 || opts.outDir) && !imagesInlined;
        try {
          output = serializeJson(Boolean(opts.imageRefs) && savesImages);
        } catch (err) {
          if (!savesImages || !result.images?.length) throw err;
          output = serializeJson(true);
          process.stderr.write(`  \u26A0\uFE0F \uC774\uBBF8\uC9C0 base64 \uC778\uB77C\uC778\uC774 \uC9C1\uB82C\uD654 \uD55C\uACC4\uB97C \uB118\uC5B4 \uD30C\uC77C \uCC38\uC870\uB85C \uB300\uCCB4\uD588\uC2B5\uB2C8\uB2E4 (${result.images.length}\uAC1C \u2192 images/)
`);
        }
      } else if (opts.format === "chunks") {
        const { blocksToChunks } = await import("./chunks-LWAOHTK2.js");
        output = JSON.stringify(blocksToChunks(result.blocks), null, 2);
      } else {
        output = markdown;
      }
      const saveImages = (dir) => {
        if (!result.images?.length || imagesInlined) return;
        const imgDir = resolve(dir, "images");
        mkdirSync(imgDir, { recursive: true });
        for (const img of result.images) {
          writeFileSync(resolve(imgDir, img.filename), img.data);
        }
        if (!opts.silent) process.stderr.write(`  \u2192 ${result.images.length}\uAC1C \uC774\uBBF8\uC9C0 \u2192 ${imgDir}
`);
      };
      if (opts.output && files.length === 1) {
        writeFileSync(opts.output, output, "utf-8");
        if (!opts.silent) process.stderr.write(`  \u2192 ${opts.output}
`);
        saveImages(resolve(opts.output, ".."));
      } else if (opts.outDir) {
        mkdirSync(opts.outDir, { recursive: true });
        const outExt = opts.format === "json" ? ".json" : opts.format === "chunks" ? ".chunks.json" : ".md";
        const outPath = resolve(opts.outDir, fileName.replace(/\.[^.]+$/, outExt));
        writeFileSync(outPath, output, "utf-8");
        if (!opts.silent) process.stderr.write(`  \u2192 ${outPath}
`);
        saveImages(opts.outDir);
      } else {
        process.stdout.write(output + "\n");
      }
    } catch (err) {
      process.stderr.write(`
[kordoc] ERROR: ${fileName} \u2014 ${sanitizeError(err)}
`);
      if (opts.format === "json") {
        process.stdout.write(JSON.stringify({
          success: false,
          fileType: detectedFormat,
          error: sanitizeError(err),
          code: classifyError(err)
        }, null, 2) + "\n");
      }
      process.exitCode = 1;
    }
  }
});
program.command("watch <dir>").description("\uB514\uB809\uD1A0\uB9AC \uAC10\uC2DC \u2014 \uC0C8 \uBB38\uC11C \uC790\uB3D9 \uBCC0\uD658").option("--webhook <url>", "\uACB0\uACFC \uC804\uC1A1 \uC6F9\uD6C5 URL").option("-d, --out-dir <dir>", "\uBCC0\uD658 \uACB0\uACFC \uCD9C\uB825 \uB514\uB809\uD1A0\uB9AC").option("-p, --pages <range>", "\uD398\uC774\uC9C0/\uC139\uC158 \uBC94\uC704").option("--format <type>", "\uCD9C\uB825 \uD615\uC2DD: markdown \uB610\uB294 json", "markdown").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (dir, opts, command) => {
  const rootOpts = program.opts();
  opts.outDir ??= rootOpts.outDir;
  opts.pages ??= rootOpts.pages;
  opts.silent ??= rootOpts.silent;
  if (command.getOptionValueSource("format") === "default" && program.getOptionValueSource("format") === "cli") {
    opts.format = rootOpts.format;
  }
  const { watchDirectory } = await import("./watch-AZIY6AWC.js");
  await watchDirectory({
    dir,
    outDir: opts.outDir,
    webhook: opts.webhook,
    format: opts.format,
    pages: opts.pages,
    silent: opts.silent
  });
});
program.command("fill [file]").description("\uC11C\uC2DD \uBB38\uC11C\uC758 \uBE48\uCE78\uC744 \uCC44\uC6CC\uC11C \uCD9C\uB825 \u2014 kordoc fill \uC2E0\uCCAD\uC11C.hwpx -f '\uC131\uBA85=\uD64D\uAE38\uB3D9,\uC804\uD654=010-1234-5678' -o \uACB0\uACFC.hwpx / \uB0B4\uC7A5 \uAE30\uC548\uBB38 \uC11C\uC2DD\uC740 kordoc fill --template gian").option("-f, --fields <pairs>", "\uCC44\uC6B8 \uD544\uB4DC (key=value \uC27C\uD45C \uAD6C\uBD84 \uB610\uB294 JSON)").option("-j, --json <path>", "\uCC44\uC6B8 \uD544\uB4DC JSON \uD30C\uC77C \uACBD\uB85C").option("-o, --output <path>", "\uCD9C\uB825 \uD30C\uC77C \uACBD\uB85C (\uD655\uC7A5\uC790\uB85C \uD3EC\uB9F7 \uACB0\uC815: .md, .hwpx)").option("--format <type>", "\uCD9C\uB825 \uD3EC\uB9F7: hwpx-preserve (\uAE30\uBCF8, \uC6D0\uBCF8 \uC2A4\uD0C0\uC77C \uBCF4\uC874), hwpx, markdown", "hwpx-preserve").option("--formats <json>", `\uD544\uB4DC\uBCC4 \uAC12 \uC11C\uC2DD JSON (\uB77C\uBCA8\u2192\uD3EC\uB9F7) \u2014 \uC608: '{"\uB0A0\uC9DC":"yy.mm.dd","\uC8FC\uBBFC\uB4F1\uB85D\uBC88\uD638":"rrn:masked"}'`).option("--template <name>", "\uB0B4\uC7A5 \uD45C\uC900 \uC11C\uC2DD \uC0AC\uC6A9 (\uD30C\uC77C \uACBD\uB85C \uBD88\uD544\uC694): gian(\uC77C\uBC18\uAE30\uC548\uBB38) | gian-simple(\uAC04\uC774\uAE30\uC548\uBB38). \uC704\uCE58 \uC778\uC790 'templates:\uC77C\uBC18\uAE30\uC548\uBB38' \uD45C\uAE30\uB3C4 \uB3D9\uC77C").option("--list-templates", "\uB0B4\uC7A5 \uD45C\uC900 \uC11C\uC2DD \uBAA9\uB85D + \uB204\uB984\uD2C0 \uD544\uB4DC \uB098\uC5F4").option("--require-unique", "\uD55C \uD0A4\uAC00 \uC11C\uC2DD\uC758 2\uACF3 \uC774\uC0C1\uC5D0 \uB9E4\uCE6D\uB418\uBA74 \uCC44\uC6B0\uC9C0 \uC54A\uACE0 \uAC70\uBD80 (\uBC18\uBCF5 \uB77C\uBCA8 \uC591\uC2DD \uC624\uC5FC \uBC29\uC9C0)").option("--mask", "\uCC44\uC6B4 \uAC12 \uBBF8\uB178\uCD9C \u2014 \uCD9C\uB825 \uD30C\uC77C \uC5C6\uC774 markdown\uC744 stdout\uC73C\uB85C \uB0BC \uB54C \uBCF8\uBB38 \uB300\uC2E0 \uC548\uB0B4\uB9CC \uD45C\uC2DC").option("--dry-run", "\uCC44\uC6B0\uC9C0 \uC54A\uACE0 \uC11C\uC2DD \uD544\uB4DC \uBAA9\uB85D\uB9CC \uCD9C\uB825").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (file, opts, command) => {
  try {
    const rootOpts = program.opts();
    opts.output ??= rootOpts.output;
    opts.silent ??= rootOpts.silent;
    if (command.getOptionValueSource("format") === "default" && program.getOptionValueSource("format") === "cli") {
      opts.format = rootOpts.format;
    }
    if (opts.listTemplates) {
      const list = [];
      for (const t of BUILTIN_TEMPLATES) {
        const fields = await extractClickHereFields(readBuiltinTemplate(t));
        list.push({ id: t.id, aliases: t.aliases, title: t.title, fields: fields.map((f) => f.name) });
      }
      process.stdout.write(JSON.stringify(list, null, 2) + "\n");
      return;
    }
    const templateName = opts.template ?? (file && /^templates?:/i.test(file) ? file : void 0);
    let arrayBuffer;
    let inputName;
    if (templateName) {
      const t = resolveBuiltinTemplate(templateName);
      if (!t) {
        process.stderr.write(`[kordoc] \uC54C \uC218 \uC5C6\uB294 \uB0B4\uC7A5 \uD15C\uD50C\uB9BF: ${templateName} (\uC0AC\uC6A9 \uAC00\uB2A5: ${BUILTIN_TEMPLATES.map((x) => `${x.id}(${x.aliases[0]})`).join(", ")})
`);
        process.exit(1);
      }
      arrayBuffer = readBuiltinTemplate(t);
      inputName = t.file;
    } else if (file) {
      const absPath = resolve(file);
      const fileSize = statSync(absPath).size;
      if (fileSize > 500 * 1024 * 1024) {
        process.stderr.write(`[kordoc] \uD30C\uC77C\uC774 \uB108\uBB34 \uD07D\uB2C8\uB2E4 (${(fileSize / 1024 / 1024).toFixed(1)}MB)
`);
        process.exit(1);
      }
      arrayBuffer = toArrayBuffer(readFileSync(absPath));
      inputName = basename(absPath);
    } else {
      process.stderr.write(`[kordoc] \uC11C\uC2DD \uD30C\uC77C \uACBD\uB85C \uB610\uB294 --template \uC744 \uC9C0\uC815\uD574\uC8FC\uC138\uC694 (\uBAA9\uB85D: kordoc fill --list-templates)
`);
      process.exit(1);
      return;
    }
    if (!opts.silent) process.stderr.write(`[kordoc] ${inputName} \uD30C\uC2F1 \uC911...
`);
    if (opts.dryRun) {
      const result2 = await parse(arrayBuffer, { keepTrailingEmptyCols: true });
      if (!result2.success) {
        process.stderr.write(`[kordoc] \uD30C\uC2F1 \uC2E4\uD328: ${result2.error}
`);
        process.exit(1);
      }
      const formInfo2 = extractFormFields(result2.blocks);
      const clickHereFields = detectFormat(arrayBuffer) === "hwpx" ? await extractClickHereFields(arrayBuffer) : [];
      if (formInfo2.fields.length === 0 && clickHereFields.length === 0) {
        process.stderr.write(`[kordoc] \uC11C\uC2DD \uD544\uB4DC\uB97C \uCC3E\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4.
`);
        process.exit(1);
      }
      process.stdout.write(JSON.stringify(
        clickHereFields.length > 0 ? { ...formInfo2, clickHereFields } : formInfo2,
        null,
        2
      ) + "\n");
      return;
    }
    let values = {};
    if (opts.json) {
      const jsonPath = resolve(opts.json);
      const jsonContent = readFileSync(jsonPath, "utf-8");
      values = JSON.parse(jsonContent);
    } else if (opts.fields) {
      const fieldsStr = opts.fields;
      if (fieldsStr.startsWith("{")) {
        values = JSON.parse(fieldsStr);
      } else {
        const pairs = fieldsStr.split(/,(?=[가-힣A-Za-z][가-힣A-Za-z\s]*=)/);
        for (const pair of pairs) {
          const eqIdx = pair.indexOf("=");
          if (eqIdx > 0) {
            const key = pair.slice(0, eqIdx).trim();
            const val = pair.slice(eqIdx + 1).trim();
            values[key] = val;
          }
        }
      }
    } else {
      process.stderr.write(`[kordoc] \uCC44\uC6B8 \uD544\uB4DC\uB97C \uC9C0\uC815\uD574\uC8FC\uC138\uC694 (-f \uB610\uB294 -j \uC635\uC158)
`);
      process.exit(1);
    }
    let inputs = values;
    if (opts.formats) {
      let formatMap;
      try {
        formatMap = JSON.parse(String(opts.formats));
      } catch {
        process.stderr.write(`[kordoc] --formats \uB294 JSON \uAC1D\uCCB4\uC5EC\uC57C \uD569\uB2C8\uB2E4 (\uB77C\uBCA8\u2192\uD3EC\uB9F7)
`);
        process.exit(1);
      }
      inputs = Object.fromEntries(Object.entries(values).map(([k, v]) => {
        const format = formatMap[k];
        return [k, format ? { value: v, format } : v];
      }));
    }
    let outputFormat = opts.format;
    if (opts.output) {
      const ext = extname(opts.output).toLowerCase();
      const explicitFormat = command.getOptionValueSource("format") === "cli" || program.getOptionValueSource("format") === "cli";
      const before = outputFormat;
      if (ext === ".hwpx") outputFormat = outputFormat === "markdown" ? "hwpx-preserve" : outputFormat;
      else if (ext === ".md") outputFormat = "markdown";
      if (explicitFormat && before !== outputFormat) {
        process.stderr.write(`[kordoc] \u26A0\uFE0F --format ${before} \uAC00 \uCD9C\uB825 \uD655\uC7A5\uC790(${ext})\uC640 \uCDA9\uB3CC\uD569\uB2C8\uB2E4 \u2014 \uD655\uC7A5\uC790 \uAE30\uC900 ${outputFormat} \uB85C \uC9C4\uD589
`);
      }
    }
    if (outputFormat === "hwpx-preserve") {
      const format = detectFormat(arrayBuffer);
      let isHwpx = format === "hwpx";
      if (isHwpx) {
        const zipFormat = await detectZipFormat(arrayBuffer);
        isHwpx = zipFormat === "hwpx";
      }
      if (!isHwpx) {
        if (!opts.silent) process.stderr.write(`[kordoc] HWPX\uAC00 \uC544\uB2C8\uBBC0\uB85C hwpx \uBAA8\uB4DC\uB85C \uC804\uD658\uD569\uB2C8\uB2E4
`);
        outputFormat = "hwpx";
      } else {
        const hwpxResult = opts.requireUnique ? await fillWithUniqueGuard(inputs, (vals, blocked) => fillHwpx(arrayBuffer, vals, blocked)) : { ...await fillHwpx(arrayBuffer, inputs), rejected: [] };
        if (!opts.silent) {
          process.stderr.write(`[kordoc] ${hwpxResult.filled.length}\uAC1C \uD544\uB4DC \uCC44\uC6C0 (\uC6D0\uBCF8 \uC2A4\uD0C0\uC77C \uBCF4\uC874)
`);
          if (hwpxResult.rejected.length > 0) {
            process.stderr.write(`[kordoc] \u26A0\uFE0F \uBAA8\uD638 \uB77C\uBCA8 \uAC70\uBD80(2\uACF3+ \uB9E4\uCE6D): ${hwpxResult.rejected.join(", ")}
`);
          }
          if (hwpxResult.unmatched.length > 0) {
            process.stderr.write(`[kordoc] \u26A0\uFE0F \uB9E4\uCE6D \uC2E4\uD328: ${hwpxResult.unmatched.join(", ")}
`);
          }
        }
        if (opts.output) {
          mkdirSync(dirname(resolve(opts.output)), { recursive: true });
          writeFileSync(resolve(opts.output), Buffer.from(hwpxResult.buffer));
          if (!opts.silent) process.stderr.write(`[kordoc] \u2192 ${resolve(opts.output)}
`);
        } else {
          process.stdout.write(Buffer.from(hwpxResult.buffer));
        }
        return;
      }
    }
    const result = await parse(arrayBuffer, { keepTrailingEmptyCols: true });
    if (!result.success) {
      process.stderr.write(`[kordoc] \uD30C\uC2F1 \uC2E4\uD328: ${result.error}
`);
      process.exit(1);
    }
    const formInfo = extractFormFields(result.blocks);
    if (!opts.silent) {
      process.stderr.write(`[kordoc] \uC11C\uC2DD \uD544\uB4DC ${formInfo.fields.length}\uAC1C \uAC10\uC9C0 (\uD655\uC2E0\uB3C4 ${(formInfo.confidence * 100).toFixed(0)}%)
`);
    }
    const fillResult = opts.requireUnique ? await fillWithUniqueGuard(inputs, (vals, blocked) => fillFormFields(result.blocks, vals, blocked)) : { ...fillFormFields(result.blocks, inputs), rejected: [] };
    if (!opts.silent) {
      process.stderr.write(`[kordoc] ${fillResult.filled.length}\uAC1C \uD544\uB4DC \uCC44\uC6C0
`);
      if (fillResult.rejected.length > 0) {
        process.stderr.write(`[kordoc] \u26A0\uFE0F \uBAA8\uD638 \uB77C\uBCA8 \uAC70\uBD80(2\uACF3+ \uB9E4\uCE6D): ${fillResult.rejected.join(", ")}
`);
      }
      if (fillResult.unmatched.length > 0) {
        process.stderr.write(`[kordoc] \u26A0\uFE0F \uB9E4\uCE6D \uC2E4\uD328: ${fillResult.unmatched.join(", ")}
`);
      }
    }
    const markdown = blocksToMarkdown(fillResult.blocks);
    if (outputFormat === "hwpx") {
      const hwpxBuffer = await markdownToHwpx(markdown);
      if (opts.output) {
        mkdirSync(dirname(resolve(opts.output)), { recursive: true });
        writeFileSync(resolve(opts.output), Buffer.from(hwpxBuffer));
        if (!opts.silent) process.stderr.write(`[kordoc] \u2192 ${resolve(opts.output)}
`);
      } else {
        process.stdout.write(Buffer.from(hwpxBuffer));
      }
    } else {
      if (opts.output) {
        mkdirSync(dirname(resolve(opts.output)), { recursive: true });
        writeFileSync(resolve(opts.output), markdown, "utf-8");
        if (!opts.silent) process.stderr.write(`[kordoc] \u2192 ${resolve(opts.output)}
`);
      } else if (opts.mask) {
        process.stdout.write(`\u26A0\uFE0F --mask \uD65C\uC131 \u2014 \uAC1C\uC778\uC815\uBCF4 \uB178\uCD9C \uBC29\uC9C0\uB97C \uC704\uD574 \uBCF8\uBB38\uC744 \uCD9C\uB825\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4. -o \uB85C \uD30C\uC77C \uC800\uC7A5 \uD6C4 \uD655\uC778\uD558\uC138\uC694.
`);
      } else {
        process.stdout.write(markdown + "\n");
      }
    }
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("seal <file>").description('\uB3C4\uC7A5/\uC11C\uBA85 \uC774\uBBF8\uC9C0\uB97C \uC575\uCEE4 \uBB38\uAD6C \uC704\uC5D0 \uBD80\uC720 \uBC30\uCE58 (\uD45C/\uD398\uC774\uC9C0 \uBD88\uD655\uC7A5) \u2014 kordoc seal \uC2E0\uCCAD\uC11C.hwpx --image \uB3C4\uC7A5.png --anchor "(\uC778)" -o \uACB0\uACFC.hwpx').requiredOption("--image <path>", "\uB3C4\uC7A5/\uC11C\uBA85 \uC774\uBBF8\uC9C0 (\uD22C\uBA85 \uBC30\uACBD PNG \uAD8C\uC7A5)").option("--anchor <text>", "\uC575\uCEE4 \uBB38\uAD6C", "(\uC778)").option("-n, --occurrence <num>", "\uAC19\uC740 \uC575\uCEE4\uAC00 \uC5EC\uB7FF\uC77C \uB54C 0-based \uC120\uD0DD", "0").option("--size-mm <num>", "\uB3C4\uC7A5 \uD55C \uBCC0 \uD06C\uAE30 mm (\uAE30\uBCF8: \uC904\uB192\uC774\xD71.6, 7~18 \uD074\uB7A8\uD504)").option("--mode <mode>", "overlap(\uBB38\uAD6C \uC704 \uACB9\uCE68) | right(\uC624\uB978\uCABD \uC606) | auto", "auto").option("--dx <mm>", "x \uBBF8\uC138\uC870\uC815 mm", "0").option("--dy <mm>", "y \uBBF8\uC138\uC870\uC815 mm", "0").option("-o, --output <path>", "\uCD9C\uB825 \uACBD\uB85C (\uAE30\uBCF8: <\uC785\uB825>.sealed.hwpx)").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (file, opts) => {
  try {
    const { placeSealHwpx, detectFormat: detectFormat2 } = await import("./-WYNJOQ6P.js");
    const rootOpts = program.opts();
    const output = opts.output ?? rootOpts.output;
    const silent = opts.silent ?? rootOpts.silent;
    const mode = String(opts.mode).toLowerCase();
    if (!["overlap", "right", "auto"].includes(mode)) {
      process.stderr.write(`[kordoc] --mode \uB294 overlap/right/auto \uC911 \uD558\uB098\uC5EC\uC57C \uD569\uB2C8\uB2E4
`);
      process.exit(1);
    }
    const buf = new Uint8Array(readFileSync(resolve(file)));
    if (detectFormat2(buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength)) !== "hwpx") {
      process.stderr.write(`[kordoc] seal \uC740 HWPX \uC804\uC6A9\uC785\uB2C8\uB2E4 (HWP 5.x \uBC14\uC774\uB108\uB9AC\uB294 \uBBF8\uC9C0\uC6D0)
`);
      process.exit(1);
    }
    const imgPath = resolve(opts.image);
    const imgSize = statSync(imgPath).size;
    if (imgSize > 500 * 1024 * 1024) {
      process.stderr.write(`[kordoc] \uB3C4\uC7A5 \uC774\uBBF8\uC9C0\uAC00 \uB108\uBB34 \uD07D\uB2C8\uB2E4 (${(imgSize / 1024 / 1024).toFixed(0)}MB)
`);
      process.exit(1);
    }
    const image = new Uint8Array(readFileSync(imgPath));
    const ext = extname(opts.image).slice(1).toLowerCase() || "png";
    const parseNum = (flag, raw, { min, allowNeg = false } = {}) => {
      const n = Number(raw);
      if (!Number.isFinite(n) || !allowNeg && n < 0 || min !== void 0 && n < min) {
        process.stderr.write(`[kordoc] ${flag} \uAC12\uC774 \uC798\uBABB\uB410\uC2B5\uB2C8\uB2E4: ${raw}${allowNeg ? "" : " (0 \uC774\uC0C1 \uC22B\uC790)"}
`);
        process.exit(1);
      }
      return n;
    };
    let sizeMm;
    if (opts.sizeMm != null) {
      const n = Number(opts.sizeMm);
      if (!Number.isFinite(n) || n <= 0) {
        process.stderr.write(`[kordoc] --size-mm \uC740 \uC591\uC218\uC5EC\uC57C \uD569\uB2C8\uB2E4: ${opts.sizeMm}
`);
        process.exit(1);
      }
      sizeMm = n;
    }
    const result = await placeSealHwpx(
      buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength),
      [{
        anchor: opts.anchor,
        occurrence: parseNum("--occurrence", opts.occurrence),
        image,
        ext,
        sizeMm,
        mode,
        dxMm: parseNum("--dx", opts.dx, { allowNeg: true }),
        dyMm: parseNum("--dy", opts.dy, { allowNeg: true })
      }]
    );
    const outPath = resolve(output ?? file.replace(/\.hwpx$/i, "") + ".sealed.hwpx");
    mkdirSync(dirname(outPath), { recursive: true });
    writeFileSync(outPath, Buffer.from(result.buffer));
    if (!silent) {
      for (const p of result.placed) {
        process.stderr.write(`[kordoc] \uB3C4\uC7A5 \uBC30\uCE58: "${p.anchor}" #${p.occurrence} \u2192 ${p.mode} (${p.posXMm}mm, ${p.posYMm}mm, ${p.sizeMm}mm) [${p.entry}]
`);
        for (const w of p.warnings ?? []) process.stderr.write(`[kordoc] \u26A0\uFE0F ${w}
`);
      }
      process.stderr.write(`[kordoc] \u2192 ${outPath}
`);
    }
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("patch <original> <edited>").description("\uC11C\uC2DD \uBCF4\uC874 \uB77C\uC6B4\uB4DC\uD2B8\uB9BD \uD328\uCE58 \u2014 \uD3B8\uC9D1\uB41C \uB9C8\uD06C\uB2E4\uC6B4\uC744 \uC6D0\uBCF8 HWPX/HWP\uC5D0 in-place \uBC18\uC601 (kordoc patch \uC6D0\uBCF8.hwpx \uD3B8\uC9D1.md -o \uCD9C\uB825.hwpx). \uBBF8\uC801\uC6A9(skip) \uD3B8\uC9D1\uC774 \uC788\uC73C\uBA74 exit 2").option("-o, --output <path>", "\uCD9C\uB825 \uACBD\uB85C (\uAE30\uBCF8: <\uC6D0\uBCF8>.patched.hwpx|.hwp)").option("--no-verify", "\uD328\uCE58 \uD6C4 \uC7AC\uD30C\uC2F1 \uC790\uB3D9 \uAC80\uC99D \uC0DD\uB7B5").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (original, edited, opts) => {
  try {
    const { patchHwpx, patchHwp, detectFormat: detectFormat2 } = await import("./-WYNJOQ6P.js");
    const rootOpts = program.opts();
    const output = opts.output ?? rootOpts.output;
    const silent = opts.silent ?? rootOpts.silent;
    const originalBuf = new Uint8Array(readFileSync(resolve(original)));
    const editedMarkdown = readFileSync(resolve(edited), "utf-8");
    const format = detectFormat2(originalBuf.buffer);
    const result = format === "hwp" ? await patchHwp(originalBuf, editedMarkdown, { verify: opts.verify !== false }) : await patchHwpx(originalBuf, editedMarkdown, { verify: opts.verify !== false });
    if (!result.success || !result.data) {
      process.stderr.write(`[kordoc] \uD328\uCE58 \uC2E4\uD328: ${result.error ?? "\uC54C \uC218 \uC5C6\uB294 \uC624\uB958"}
`);
      process.exit(1);
    }
    const ext = format === "hwp" ? ".hwp" : ".hwpx";
    const outPath = resolve(output ?? original.replace(/\.hwpx?$/i, "") + ".patched" + ext);
    mkdirSync(dirname(outPath), { recursive: true });
    writeFileSync(outPath, result.data);
    if (!silent) {
      process.stderr.write(`[kordoc] ${result.applied}\uAC1C \uBCC0\uACBD \uC801\uC6A9 (\uC6D0\uBCF8 \uC11C\uC2DD \uBCF4\uC874) \u2192 ${outPath}
`);
      for (const s of result.skipped) {
        process.stderr.write(`[kordoc] \u26A0\uFE0F SKIP: ${s.reason}${s.before ? ` | ${s.before}` : ""}
`);
      }
      if (result.verification) {
        const v = result.verification.stats;
        const residual = v.added + v.removed + v.modified;
        process.stderr.write(residual === 0 ? `[kordoc] \u2713 \uAC80\uC99D: \uD3B8\uC9D1 \uB9C8\uD06C\uB2E4\uC6B4\uACFC \uC7AC\uD30C\uC2F1 \uACB0\uACFC \uC644\uC804 \uC77C\uCE58 (${v.unchanged}\uBE14\uB85D)
` : `[kordoc] \u26A0\uFE0F \uAC80\uC99D \uC794\uCC28: \uC218\uC815 ${v.modified}, \uCD94\uAC00 ${v.added}, \uC0AD\uC81C ${v.removed} (\uBBF8\uC9C0\uC6D0 \uBCC0\uACBD\uC740 skip \uBAA9\uB85D \uCC38\uC870)
`);
      }
    }
    if (result.skipped.length > 0) process.exitCode = 2;
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("validate <file>").description("HWPX \uAD6C\uC870 \uAC80\uC99D \u2014 ZIP\xB7mimetype\xB7\uD544\uC218 \uD30C\uC77C\xB7XML \uC6F0\uD3FC\uB4DC\xB7secCnt\xB7manifest \uCC38\uC870 (\uD55C\uCEF4\uB3C5\uC2A4 \uAC70\uBD80 \uC694\uC778 \uC0AC\uC804 \uCC28\uB2E8)").option("--json", "\uACB0\uACFC\uB97C JSON\uC73C\uB85C stdout\uC5D0 \uCD9C\uB825").action(async (file, opts) => {
  try {
    const { validateHwpx } = await import("./-WYNJOQ6P.js");
    const buf = new Uint8Array(readFileSync(resolve(file)));
    const result = await validateHwpx(buf);
    if (opts.json) {
      process.stdout.write(JSON.stringify(result, null, 2) + "\n");
    } else if (result.ok) {
      process.stderr.write(`[kordoc] \u2713 \uAD6C\uC870 \uAC80\uC99D \uD1B5\uACFC (\uC5D4\uD2B8\uB9AC ${result.entryCount}\uAC1C): ${file}
`);
    } else {
      process.stderr.write(`[kordoc] \u2717 \uAD6C\uC870 \uBB38\uC81C ${result.issues.length}\uAC74: ${file}
`);
      for (const i of result.issues) {
        process.stderr.write(`[kordoc]   - ${i.path ? `${i.path}: ` : ""}${i.message}
`);
      }
    }
    if (!result.ok) process.exit(1);
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("generate <markdown>").alias("gen").description("\uB9C8\uD06C\uB2E4\uC6B4 \u2192 \uACF5\uBB38\uC11C HWPX \uC0DD\uC131 \u2014 kordoc generate \uBCF4\uACE0\uC11C.md -o \uBCF4\uACE0\uC11C.hwpx --preset \uBCF4\uACE0\uC11C (markdown\uC5D0 '-' \uC9C0\uC815 \uC2DC stdin)").option("-o, --output <path>", "\uCD9C\uB825 HWPX \uACBD\uB85C (\uAE30\uBCF8: <\uC785\uB825>.hwpx)").option("--preset <name>", "\uACF5\uBB38\uC11C \uD504\uB9AC\uC14B: \uAE30\uC548\uBB38(official)\xB7\uBCF4\uACE0\uC11C(report)\xB7\uACC4\uD68D\uC11C(plan)\xB7\uD1B5\uC9C0(notice)\xB7\uD68C\uC758\uB85D(minutes)\xB7\uAC1C\uC870\uC2DD(gaejosik \u2014 \uD45C\uC9C0\xB7\uBAA9\uCC28\xB7\uC7A5\uD5E4\uB354 \uC790\uB3D9)\xB7\uBCF4\uB3C4\uC790\uB8CC(press)", "\uAE30\uC548\uBB38").option("--font <type>", "\uBCF8\uBB38 \uAE00\uAF34: myeongjo(\uD568\uCD08\uB86C\uBC14\uD0D5) \uB610\uB294 gothic(\uB9D1\uC740 \uACE0\uB515)").option("--pt <size>", "\uBCF8\uBB38 \uAE00\uC790 \uD06C\uAE30(pt)").option("--line-spacing <percent>", "\uBCF8\uBB38 \uC904\uAC04\uACA9(%)").option("--profile <path>", "\uC11C\uC2DD \uD504\uB85C\uD544 JSON (kordoc profile\uB85C \uCD94\uCD9C) \u2014 \uCC38\uC870 \uBB38\uC11C\uC758 \uD45C \uD14C\uB450\uB9AC\xB7\uC74C\uC601\xB7\uC5F4\uD3ED\xB7\uC140 \uAE00\uAF34 \uC7AC\uD604").option("--org <name>", "\uD45C\uC9C0 \uAE30\uAD00\uBA85 (\uD45C\uC9C0\uB97C \uCF1C\uB294 \uD504\uB9AC\uC14B \uACF5\uD1B5)").option("--date <date>", "\uD45C\uC9C0 \uB0A0\uC9DC (\uAE30\uBCF8 \uC624\uB298 \u2014 'YYYY. M. D.')").option("--toc", "\uBAA9\uCC28 \uD398\uC774\uC9C0 \uAC15\uC81C \uCF1C\uAE30 (\uAC1C\uC870\uC2DD \uC678 \uD504\uB9AC\uC14B\uC5D0\uC11C\uB3C4 h2 \uBAA9\uB85D\uC73C\uB85C \uC0DD\uC131)").option("--no-toc", "\uBAA9\uCC28 \uD398\uC774\uC9C0 \uB044\uAE30 (\uAC1C\uC870\uC2DD \uD504\uB9AC\uC14B \uAE30\uBCF8 \uCF1C\uC9D0)").option("--cover", "\uD45C\uC9C0 \uD398\uC774\uC9C0 \uAC15\uC81C \uCF1C\uAE30 (\uAC1C\uC870\uC2DD \uC678 \uD504\uB9AC\uC14B\uC5D0\uC11C\uB3C4 \uCCAB h1\uC744 \uD45C\uC9C0\uB85C)").option("--no-cover", "\uD45C\uC9C0 \uD398\uC774\uC9C0 \uB044\uAE30 (\uAC1C\uC870\uC2DD \uD504\uB9AC\uC14B \uAE30\uBCF8 \uCF1C\uC9D0)").option("--approval <labels>", "\uACB0\uC7AC\uB780 \uC9C1\uC704 \uB77C\uBCA8 (\uC27C\uD45C \uAD6C\uBD84, \uC608: \uB2F4\uB2F9,\uD300\uC7A5,\uACFC\uC7A5) \u2014 \uBB38\uC11C \uCD5C\uC0C1\uB2E8 \uC6B0\uCE21").option("--page-numbers", "\uCABD\uBC88\uD638 \uAC15\uC81C \uCF1C\uAE30 (\uD558\uB2E8 \uC911\uC559 '- 1 -')").option("--no-page-numbers", "\uCABD\uBC88\uD638 \uB044\uAE30 (\uAC1C\uC870\uC2DD\xB7\uBCF4\uACE0\uC11C\xB7\uACC4\uD68D\uC11C \uAE30\uBCF8 \uCF1C\uC9D0)").option("--end-mark", "\uBCF8\uBB38 \uB05D '\uB05D.' \uD45C\uC2DC \uAC15\uC81C \uCF1C\uAE30").option("--no-end-mark", "'\uB05D.' \uD45C\uC2DC \uB044\uAE30 (\uAE30\uC548\uBB38 \uAE30\uBCF8 \uCF1C\uC9D0)").option("--no-body-title-box", "\uBCF8\uBB38 \uCCAB \uD398\uC774\uC9C0 \uC81C\uBAA9 \uBC18\uBCF5 \uBC15\uC2A4 \uB044\uAE30 (\uAC1C\uC870\uC2DD+\uD45C\uC9C0 \uAE30\uBCF8 \uCF1C\uC9D0)").option("--h2-marker <type>", "h2 \uC139\uC158 \uC81C\uBAA9 \uB9D0\uBA38\uB9AC: box(\u25A1 \u2014 \uBCF4\uACE0\uC11C\xB7\uACC4\uD68D\uC11C \uAE30\uBCF8)\xB7number(1. \u2014 \uACF5\uACE0\uBB38 \uAD00\uD589)\xB7none").option("--fonts <spec>", "\uC694\uC18C\uBCC4 \uAE00\uAF34 \uC624\uBC84\uB77C\uC774\uB4DC: body=\uB098\uB214\uBA85\uC870,heading=\uB098\uB214\uACE0\uB515,ref=\uD55C\uC591\uC911\uACE0\uB515,table=\uB9D1\uC740 \uACE0\uB515").option("--sizes <spec>", "\uAC1C\uC870\uC2DD \uC694\uC18C\uBCC4 \uD06C\uAE30(pt): dae=16,cham=13,table=12,coverTitle=30 \u2026").option("--bullet2 <char>", "2\uB2E8\uACC4 \uD56D\uBAA9\uBD80\uD638: \u3147(\uC774\uC751 \u2014 \uAE30\uC548\uBB38\xB7\uACF5\uACE0\uBB38 \uC2E4\uCE21 \uC9C0\uBC30) \uB610\uB294 \u25CB(\uC6D0 \u2014 \uBCF4\uACE0\uC11C \uC591\uC2DD)").option("--suppress-single", "\uB2E8\uC77C \uD615\uC81C \uD56D\uBAA9 \uBD80\uD638 \uC0DD\uB7B5 (\uD3B8\uB78C \uADDC\uC815 \u2014 \uAE30\uBCF8\uC740 \uD558\uB098\uC5EC\uB3C4 \uBD80\uD638 \uBD80\uC5EC)").option("--doc-head <spec>", "\uAE30\uC548\uBB38 \uB450\uBB38: org=\uD589\uC815\uC548\uC804\uBD80,to=\uC218\uC2E0\uCC98,title=\uC81C\uBAA9 (\uBCC4\uC9C0 \uC81C1\uD638\uC11C\uC2DD)").option("--doc-foot <spec>", "\uAE30\uC548\uBB38 \uACB0\uBB38: sender=\uC7A5\uAD00\uBA85\uC758,drafter=\uAE30\uC548\uC790,reviewer=\uAC80\uD1A0\uC790,approver=\uACB0\uC7AC\uAD8C\uC790,docNum=\uC2DC\uD589\uBC88\uD638,phone=\uC804\uD654,email=\uBA54\uC77C,disclosure=\uACF5\uAC1C\uAD6C\uBD84 \u2026").option("--report-info <text>", "\uC5C5\uBB34\uBCF4\uACE0 \uC6B0\uC0C1\uB2E8 \uBCF4\uACE0\uC815\uBCF4 \uD589 \u2014 \uC608: '(2026. 7. 11., \uACFC\uC7A5 \uD64D\uAE38\uB3D9, \u260E02-120)'").option("--notice-head <spec>", "\uACF5\uACE0\uBB38 \uB450\uBB38\xB7\uACB0\uBB38: no=\uACF5\uACE0 \uC81C2026-1\uD638,date=2026\uB144 7\uC6D4 11\uC77C,sender=\uD589\uC815\uC548\uC804\uBD80\uC7A5\uAD00").option("--press-head <spec>", "\uBCF4\uB3C4\uC790\uB8CC \uBA38\uB9AC: release=\uBCF4\uB3C4\uC2DC\uC810,distribute=\uBC30\uD3EC\uC77C,dept=\uB2F4\uB2F9\uBD80\uC11C,manager=\uB2F4\uB2F9\uC790,phone=\uC5F0\uB77D\uCC98").option("--press-sub <items>", "\uBCF4\uB3C4\uC790\uB8CC \uBD80\uC81C (\uC138\uBBF8\uCF5C\uB860 \uAD6C\uBD84, \uC81C\uBAA9 \uC544\uB798 '- \u2026 -')").option("--plain", "\uACF5\uBB38\uC11C \uBAA8\uB4DC \uB044\uAE30 (\uBC94\uC6A9 \uB9C8\uD06C\uB2E4\uC6B4 \uBCC0\uD658)").option("--paper <size>", "\uC6A9\uC9C0: A4\xB7A3\xB7B4\xB7B5\xB7Letter \uB610\uB294 '210x297'(mm)").option("--landscape", "\uC6A9\uC9C0 \uAC00\uB85C \uBC29\uD5A5").option("--columns <n>", "\uB2E4\uB2E8 \uAC1C\uC218 (1~8)").option("--header <text>", "\uBA38\uB9AC\uB9D0 \uD14D\uC2A4\uD2B8 (\uBAA8\uB4E0 \uCABD, \uC778\uB77C\uC778 \uB9C8\uD06C\uB2E4\uC6B4 \uD5C8\uC6A9)").option("--footer <text>", "\uAF2C\uB9AC\uB9D0 \uD14D\uC2A4\uD2B8").option("--image-dir <dir>", "\uB9C8\uD06C\uB2E4\uC6B4 \uC774\uBBF8\uC9C0 \uCC38\uC870(![](x.png))\uB97C \uC774 \uB514\uB809\uD1A0\uB9AC\uC5D0\uC11C \uC77D\uC5B4 \uC2E4\uB370\uC774\uD130 \uC784\uBCA0\uB4DC").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (markdown, opts) => {
  try {
    const rootOpts = program.opts();
    const output = opts.output ?? rootOpts.output;
    const silent = opts.silent ?? rootOpts.silent;
    let md;
    let baseName = "document";
    if (markdown === "-") {
      md = readFileSync(0, "utf-8");
    } else {
      const inPath = resolve(markdown);
      md = readFileSync(inPath, "utf-8");
      baseName = basename(inPath).replace(/\.(md|markdown|txt)$/i, "");
    }
    let gongmun;
    if (!opts.plain) {
      const preset = PRESET_ALIAS[String(opts.preset).trim()];
      if (!preset) {
        process.stderr.write(`[kordoc] \uC54C \uC218 \uC5C6\uB294 \uD504\uB9AC\uC14B: ${opts.preset} (\uAE30\uC548\uBB38/\uBCF4\uACE0\uC11C/\uACC4\uD68D\uC11C/\uD1B5\uC9C0/\uD68C\uC758\uB85D/\uAC1C\uC870\uC2DD/\uBCF4\uB3C4\uC790\uB8CC)
`);
        process.exit(1);
      }
      const enumCheck = (flag, value, allowed) => {
        if (value === void 0) return void 0;
        if (!allowed.includes(String(value))) {
          process.stderr.write(`[kordoc] ${flag} \uB294 ${allowed.join("/")}
`);
          process.exit(1);
        }
        return value;
      };
      const parseKv = (spec, flag) => {
        const out = {};
        for (const piece of spec.split(",")) {
          const p = piece.trim();
          if (!p) continue;
          const eq = p.indexOf("=");
          const key = eq > 0 ? p.slice(0, eq).trim() : "";
          const value = eq > 0 ? p.slice(eq + 1).trim() : "";
          if (!key || !value) {
            process.stderr.write(`[kordoc] ${flag}: 'key=value' \uD615\uC2DD\uC774 \uC544\uB2CC \uC870\uAC01 \uBB34\uC2DC \u2014 "${p}" (\uAC12\uC5D0 \uC27C\uD45C\uB294 \uC4F8 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4)
`);
            continue;
          }
          out[key] = value;
        }
        return out;
      };
      const pressKv = opts.pressHead ? parseKv(String(opts.pressHead), "--press-head") : {};
      gongmun = buildGongmunOptions({
        preset,
        font: enumCheck("--font", opts.font, BODY_FONTS),
        bodyPt: opts.pt ? Number(opts.pt) : void 0,
        lineSpacing: opts.lineSpacing ? Number(opts.lineSpacing) : void 0,
        org: opts.org,
        date: opts.date,
        cover: opts.cover,
        toc: opts.toc,
        approval: opts.approval ? String(opts.approval).split(",").map((s) => s.trim()).filter(Boolean) : void 0,
        pageNumbers: opts.pageNumbers,
        endMark: opts.endMark,
        // --no-body-title-box 단독 플래그 — commander 기본 true는 "미지정"으로 정돈
        bodyTitleBox: opts.bodyTitleBox === false ? false : void 0,
        h2Marker: enumCheck("--h2-marker", opts.h2Marker, H2_MARKERS),
        fonts: opts.fonts ? parseKv(String(opts.fonts), "--fonts") : void 0,
        sizes: opts.sizes ? Object.fromEntries(
          Object.entries(parseKv(String(opts.sizes), "--sizes")).map(([k, v]) => [k, Number(v)]).filter(([, v]) => Number.isFinite(v))
        ) : void 0,
        bullet2: enumCheck("--bullet2", opts.bullet2, BULLET2_CHARS),
        suppressSingle: opts.suppressSingle ? true : void 0,
        docHead: opts.docHead ? parseKv(String(opts.docHead), "--doc-head") : void 0,
        docFoot: opts.docFoot ? parseKv(String(opts.docFoot), "--doc-foot") : void 0,
        reportInfo: opts.reportInfo ? String(opts.reportInfo) : void 0,
        noticeHead: opts.noticeHead ? parseKv(String(opts.noticeHead), "--notice-head") : void 0,
        press: opts.pressHead || opts.pressSub ? {
          release: pressKv.release,
          distribute: pressKv.distribute,
          sub: opts.pressSub ? String(opts.pressSub).split(";").map((s) => s.trim()).filter(Boolean) : void 0,
          contact: pressKv.dept || pressKv.manager || pressKv.phone ? { dept: pressKv.dept, manager: pressKv.manager, phone: pressKv.phone } : void 0
        } : void 0
      });
    }
    if (gongmun?.fonts && !silent) {
      for (const w of unknownFontWarnings(gongmun.fonts)) process.stderr.write(`[kordoc] ${w}
`);
    }
    if (gongmun && !silent) {
      for (const w of incompatibleGongmunWarnings(gongmun)) process.stderr.write(`[kordoc] \u26A0 ${w}
`);
    }
    if (gongmun && !silent) {
      for (const w of gongmunLintWarnings(md, 5)) process.stderr.write(`[kordoc] \u26A0 ${w}
`);
    }
    let profile;
    if (opts.profile) {
      profile = parseFormatProfileJson(readFileSync(resolve(String(opts.profile)), "utf-8"));
      if (!silent) process.stderr.write(`[kordoc] \uC11C\uC2DD \uD504\uB85C\uD544 \uC801\uC6A9: \uD45C ${profile.tables.length}\uAC1C (${opts.profile})
`);
    }
    let page;
    if (opts.paper || opts.landscape || opts.columns || opts.header || opts.footer) {
      let size;
      if (opts.paper) {
        const wh = /^(\d+(?:\.\d+)?)x(\d+(?:\.\d+)?)$/i.exec(String(opts.paper).trim());
        size = wh ? { widthMm: Number(wh[1]), heightMm: Number(wh[2]) } : String(opts.paper);
      }
      page = {
        ...size !== void 0 ? { size } : {},
        ...opts.landscape ? { orientation: "landscape" } : {},
        ...opts.columns ? { columns: Number(opts.columns) } : {},
        ...opts.header ? { header: String(opts.header) } : {},
        ...opts.footer ? { footer: String(opts.footer) } : {}
      };
    }
    let imageBytes;
    if (opts.imageDir) {
      const dir = resolve(String(opts.imageDir));
      for (const m of md.matchAll(/!\[[^\]]*\]\(([^)\s]+)\)/g)) {
        const url = m[1];
        if (!/^[A-Za-z0-9._-]+\.[A-Za-z0-9]+$/.test(url) || url.includes("..")) continue;
        try {
          const bytes = readFileSync(resolve(dir, url));
          (imageBytes ??= {})[url] = new Uint8Array(bytes);
        } catch {
        }
      }
      if (!silent) process.stderr.write(`[kordoc] \uC774\uBBF8\uC9C0 \uC784\uBCA0\uB4DC: ${Object.keys(imageBytes ?? {}).length}\uAC1C (${dir})
`);
    }
    const buf = await markdownToHwpx(md, gongmun || profile || page || imageBytes ? {
      ...gongmun ? { gongmun } : {},
      ...profile ? { profile } : {},
      ...page ? { page } : {},
      ...imageBytes ? { images: imageBytes } : {}
    } : void 0);
    const outPath = resolve(output ?? (markdown === "-" ? `${baseName}.hwpx` : markdown.replace(/\.(md|markdown|txt)$/i, "") + ".hwpx"));
    mkdirSync(dirname(outPath), { recursive: true });
    writeFileSync(outPath, Buffer.from(buf));
    if (!silent) {
      const mode = gongmun ? `\uACF5\uBB38\uC11C:${gongmun.preset}` : "\uBC94\uC6A9";
      process.stderr.write(`[kordoc] HWPX \uC0DD\uC131 (${mode}) \u2192 ${outPath}
`);
    }
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("profile <file>").description("HWPX \uD45C \uC11C\uC2DD \uD504\uB85C\uD544 \uCD94\uCD9C \u2014 \uCC38\uC870 \uBB38\uC11C\uC758 \uD45C \uD14C\uB450\uB9AC\xB7\uC74C\uC601\xB7\uC5F4\uD3ED\xB7\uC140 \uAE00\uAF34\uC744 JSON\uC73C\uB85C (generate --profile\uB85C \uC7AC\uD604) \u2014 kordoc profile \uCC38\uC870.hwpx -o \uC11C\uC2DD.json").option("-o, --output <path>", "\uCD9C\uB825 JSON \uACBD\uB85C (\uAE30\uBCF8: <\uC785\uB825>.profile.json)").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (file, opts) => {
  try {
    const rootOpts = program.opts();
    const output = opts.output ?? rootOpts.output;
    const silent = opts.silent ?? rootOpts.silent;
    const absPath = resolve(file);
    const profile = await hwpxToProfile(readFileSync(absPath));
    const outPath = resolve(output ?? absPath.replace(/\.hwpx$/i, "") + ".profile.json");
    mkdirSync(dirname(outPath), { recursive: true });
    writeFileSync(outPath, JSON.stringify(profile, null, 2));
    if (!silent) process.stderr.write(`[kordoc] \uC11C\uC2DD \uD504\uB85C\uD544 \uCD94\uCD9C: \uD45C ${profile.tables.length}\uAC1C \u2192 ${outPath}
`);
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("lint <file>").description("\uACF5\uBB38\uC11C \uD45C\uAE30\uBC95 \uAC80\uC218 \u2014 \uB0A0\uC9DC\xB7\uC2DC\uAC04\xB7\uAE08\uC561\xB7\uBD99\uC784 \uB4F1 \uD589\uC815\uC5C5\uBB34\uC6B4\uC601 \uD3B8\uB78C \uD45C\uAE30\uBC95 (md/txt, '-'=stdin). error \uC788\uC73C\uBA74 exit 1").option("--json", "JSON \uCD9C\uB825").action((file, opts) => {
  try {
    const text = file === "-" ? readFileSync(0, "utf-8") : readFileSync(resolve(file), "utf-8");
    const findings = lintGongmunText(text);
    const errors = findings.filter((f) => f.severity === "error").length;
    if (opts.json) {
      process.stdout.write(JSON.stringify({ findings, summary: { total: findings.length, errors, ok: errors === 0 } }, null, 2) + "\n");
    } else {
      process.stderr.write(`[kordoc] \uD45C\uAE30\uBC95 \uAC80\uC218: \uC704\uBC18 ${findings.length}\uAC74 (error ${errors}, warning ${findings.length - errors})
`);
      for (const f of findings) {
        process.stderr.write(`  L${f.line} [${f.severity}] ${f.rule}: "${f.match}" \u2014 ${f.message}${f.suggest ? ` \u2192 ${f.suggest}` : ""}
`);
      }
    }
    process.exit(errors > 0 ? 1 : 0);
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("redact <files...>").description("\uAC1C\uC778\uC815\uBCF4 \uC11C\uC2DD \uBCF4\uC874 \uB9C8\uC2A4\uD0B9 \u2014 \uC8FC\uBBFC\uBC88\uD638\xB7\uC804\uD654\xB7\uC774\uBA54\uC77C\xB7\uCE74\uB4DC\xB7\uACC4\uC88C\uB97C \uD0D0\uC9C0\uD574 HWPX/HWP\uB294 \uC6D0\uBCF8 \uC11C\uC2DD \uADF8\uB300\uB85C patch, \uADF8 \uC678 \uD3EC\uB9F7\uC740 \uB9C8\uC2A4\uD0B9\uB41C \uB9C8\uD06C\uB2E4\uC6B4 \uCD9C\uB825. \uC790\uB3D9 \uAC80\uCD9C \uBCF4\uC870 \uB3C4\uAD6C\uC774\uBBC0\uB85C \uACB0\uACFC\uB294 \uBC18\uB4DC\uC2DC \uC0AC\uB78C\uC774 \uCD5C\uC885 \uD655\uC778\uD558\uC138\uC694 (\uC774\uBBF8\uC9C0 \uC548 \uD14D\uC2A4\uD2B8\uB294 \uD0D0\uC9C0 \uBD88\uAC00)").option("--rules <csv>", "\uC801\uC6A9 \uB8F0 (\uAE30\uBCF8: rrn,phone,email,card,account \u2014 passport,driver\uB294 opt-in)").option("--mask-char <ch>", "\uB9C8\uC2A4\uD06C \uBB38\uC790 1\uAE00\uC790 (\uAE30\uBCF8: \u25CF)").option("-o, --output <path>", "\uCD9C\uB825 \uACBD\uB85C (\uB2E8\uC77C \uD30C\uC77C \uC2DC)").option("-d, --out-dir <dir>", "\uCD9C\uB825 \uB514\uB809\uD1A0\uB9AC (\uB2E4\uC911 \uD30C\uC77C \uC2DC)").option("--dry-run", "\uD0D0\uC9C0 \uB9AC\uD3EC\uD2B8\uB9CC \uCD9C\uB825, \uD30C\uC77C \uBBF8\uC0DD\uC131").option("--json", "\uB9AC\uD3EC\uD2B8\uB97C JSON\uC73C\uB85C stdout \uCD9C\uB825").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (files, opts) => {
  const { redactMarkdown, DEFAULT_REDACT_RULES, patchHwpx, patchHwp } = await import("./-WYNJOQ6P.js");
  const rootOpts = program.opts();
  const output = opts.output ?? rootOpts.output;
  const silent = opts.silent ?? rootOpts.silent;
  const KNOWN_RULES = /* @__PURE__ */ new Set(["rrn", "phone", "email", "card", "account", "passport", "driver"]);
  const rules = opts.rules ? String(opts.rules).split(",").map((r) => r.trim()).filter(Boolean) : [...DEFAULT_REDACT_RULES];
  const badRule = rules.find((r) => !KNOWN_RULES.has(r));
  if (badRule) {
    process.stderr.write(`[kordoc] \uC54C \uC218 \uC5C6\uB294 \uB8F0: ${badRule} (\uD5C8\uC6A9: ${[...KNOWN_RULES].join(", ")})
`);
    process.exit(1);
  }
  if (output && files.length > 1) {
    process.stderr.write(`[kordoc] \u26A0\uFE0F -o/--output \uC740 \uB2E8\uC77C \uD30C\uC77C \uC804\uC6A9\uC774\uB77C \uBB34\uC2DC\uB429\uB2C8\uB2E4 \u2014 \uB2E4\uC911 \uD30C\uC77C\uC740 -d/--out-dir \uB97C \uC0AC\uC6A9\uD558\uC138\uC694
`);
  }
  const jsonReports = [];
  for (const filePath of files) {
    const absPath = resolve(filePath);
    const fileName = basename(absPath);
    try {
      const buffer = readFileSync(absPath);
      const arrayBuffer = toArrayBuffer(buffer);
      const format = detectFormat(arrayBuffer);
      const parsed = await parse(arrayBuffer, { filePath: absPath });
      if (!parsed.success) {
        process.stderr.write(`[kordoc] FAIL: ${fileName} \u2014 ${parsed.error}
`);
        process.exitCode = 1;
        continue;
      }
      const r = redactMarkdown(parsed.markdown, { rules, maskChar: opts.maskChar });
      const byRule = /* @__PURE__ */ new Map();
      for (const h of r.hits) byRule.set(h.rule, (byRule.get(h.rule) ?? 0) + 1);
      const ruleSummary = [...byRule.entries()].map(([k, v]) => `${k} ${v}\uAC74`).join(", ") || "0\uAC74";
      let outPath = null;
      let patchNote = "";
      if (!opts.dryRun && r.hits.length > 0) {
        const patchable = format === "hwpx" || format === "hwp";
        if (patchable) {
          const original = new Uint8Array(arrayBuffer);
          const result = format === "hwp" ? await patchHwp(original, r.text) : await patchHwpx(original, r.text);
          if (!result.success || !result.data) {
            process.stderr.write(`[kordoc] \uD328\uCE58 \uC2E4\uD328: ${fileName} \u2014 ${result.error ?? "\uC54C \uC218 \uC5C6\uB294 \uC624\uB958"}
`);
            process.exitCode = 1;
            continue;
          }
          const ext = format === "hwp" ? ".hwp" : ".hwpx";
          outPath = resolve(
            output && files.length === 1 ? output : resolve(opts.outDir ?? dirname(absPath), fileName.replace(/\.[^.]+$/, "") + ".redacted" + ext)
          );
          mkdirSync(dirname(outPath), { recursive: true });
          writeFileSync(outPath, result.data);
          if (result.skipped.length > 0) {
            patchNote = ` (\u26A0\uFE0F \uBBF8\uC801\uC6A9 ${result.skipped.length}\uAC74 \u2014 \uD574\uB2F9 \uC704\uCE58\uB294 \uC6D0\uBB38 \uC794\uC874, \uC218\uB3D9 \uD655\uC778 \uD544\uC694)`;
            process.exitCode = 2;
          }
        } else {
          outPath = resolve(
            output && files.length === 1 ? output : resolve(opts.outDir ?? dirname(absPath), fileName.replace(/\.[^.]+$/, "") + ".redacted.md")
          );
          mkdirSync(dirname(outPath), { recursive: true });
          writeFileSync(outPath, r.text, "utf-8");
          patchNote = ` (${format}\uB294 \uC11C\uC2DD \uBCF4\uC874 \uBBF8\uC9C0\uC6D0 \u2014 \uB9C8\uC2A4\uD0B9\uB41C \uB9C8\uD06C\uB2E4\uC6B4\uC73C\uB85C \uCD9C\uB825)`;
        }
      }
      if (opts.json) {
        jsonReports.push({ file: absPath, format, rules, hits: r.hits, output: outPath });
      }
      if (!silent) {
        process.stderr.write(`[kordoc] ${fileName}: ${ruleSummary}${outPath ? ` \u2192 ${outPath}` : opts.dryRun ? " (dry-run)" : ""}${patchNote}
`);
        for (const h of r.hits) process.stderr.write(`  - [${h.rule}] ${h.masked}
`);
      }
    } catch (err) {
      process.stderr.write(`[kordoc] ERROR: ${fileName} \u2014 ${sanitizeError(err)}
`);
      process.exitCode = 1;
    }
  }
  if (opts.json) process.stdout.write(JSON.stringify(files.length === 1 ? jsonReports[0] ?? null : jsonReports, null, 2) + "\n");
});
program.command("render <file>").description("\uB808\uC774\uC544\uC6C3 \uBCF4\uC874 \uB80C\uB354 \u2014 HWPX\uB97C SVG\uB85C (\uC804\uCCB4 \uD398\uC774\uC9C0 \uC138\uB85C \uC2A4\uD0DD). \uD55C\uCEF4 \uC800\uC7A5\uBCF8\uC740 \uC870\uD310 \uCE90\uC2DC \uADF8\uB300\uB85C, \uCE90\uC2DC \uC5C6\uB294 \uC0DD\uC131\uBCF8\xB7\uD3B8\uC9D1\uBCF8\uC740 \uC21C\uC218 TS \uC870\uD310(reflow, \uAE30\uBCF8 \uCF2C) \u2014 kordoc render \uBB38\uC11C.hwpx -o \uBB38\uC11C.svg").option("-o, --output <path>", "\uCD9C\uB825 SVG \uACBD\uB85C (\uAE30\uBCF8: <\uC785\uB825>.svg)").option("--highlight <terms>", "\uAC80\uC0C9\uC5B4 \uD615\uAD11\uD39C (\uC27C\uD45C \uAD6C\uBD84)").option("--no-reflow", "\uC21C\uC218 TS \uC870\uD310 \uB044\uAE30 (\uC870\uD310 \uCE90\uC2DC \uC5C6\uB294 \uBB38\uC11C\uAC00 \uBE48 \uD398\uC774\uC9C0\uB85C \uB098\uC62C \uC218 \uC788\uC74C)").option("--reflow-mode <mode>", "reflow \uC904\uBC14\uAFC8 \uBAA8\uB4DC: keep(\uC5B4\uC808) | charAll(\uAE00\uC790)", "keep").option("--silent", "\uC9C4\uD589 \uBA54\uC2DC\uC9C0 \uC228\uAE30\uAE30").action(async (file, opts) => {
  try {
    const rootOpts = program.opts();
    const output = opts.output ?? rootOpts.output;
    const silent = opts.silent ?? rootOpts.silent;
    const { renderHwpxToSvg } = await import("./render-5Y2US4IM.js");
    const absPath = resolve(file);
    const buffer = readFileSync(absPath);
    const highlights = opts.highlight ? String(opts.highlight).split(",") : void 0;
    const result = await renderHwpxToSvg(toArrayBuffer(buffer), { highlights, reflow: opts.reflow, reflowMode: opts.reflowMode });
    const outPath = resolve(output ?? file.replace(/\.hwpx$/i, "") + ".svg");
    mkdirSync(dirname(outPath), { recursive: true });
    writeFileSync(outPath, result.svg, "utf-8");
    if (!silent) {
      process.stderr.write(`[kordoc] \uB80C\uB354 (${result.pageCount}\uD398\uC774\uC9C0, ${result.width}x${result.height}pt, \uD14D\uC2A4\uD2B8 ${result.stats.texts}\xB7\uC774\uBBF8\uC9C0 ${result.stats.images}\xB7\uD45C ${result.stats.tables}) \u2192 ${outPath}
`);
      for (const w of result.warnings) process.stderr.write(`[kordoc] \u26A0\uFE0F ${w}
`);
    }
  } catch (err) {
    process.stderr.write(`[kordoc] \uC624\uB958: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("render-worker").description("persistent \uB80C\uB354 \uC6CC\uCEE4 \u2014 stdin NDJSON \uC694\uCCAD \u2192 \uC870\uD310 SVG \uD30C\uC77C \uCD9C\uB825 (\uD504\uB85C\uC138\uC2A4 \uC720\uC9C0, \uCF5C\uB4DC\uC2A4\uD0C0\uD2B8 \uC81C\uAC70)").action(async () => {
  const { createInterface } = await import("readline");
  const { renderHwpxToSvg } = await import("./render-5Y2US4IM.js");
  const rl = createInterface({ input: process.stdin });
  const write = (o) => void process.stdout.write(JSON.stringify(o) + "\n");
  write({ ready: true, version: VERSION });
  for await (const line of rl) {
    const t = line.trim();
    if (!t) continue;
    let req;
    try {
      req = JSON.parse(t);
    } catch {
      write({ ok: false, error: "\uC798\uBABB\uB41C JSON \uB77C\uC778" });
      continue;
    }
    if (req === null || typeof req !== "object") {
      write({ ok: false, error: "JSON \uAC1D\uCCB4\uAC00 \uC544\uB2D9\uB2C8\uB2E4" });
      continue;
    }
    if (req.cmd === "quit") {
      rl.close();
      break;
    }
    const id = req.id;
    try {
      if (!req.file || !req.out) throw new Error("file\xB7out \uD544\uC218");
      const buffer = readFileSync(resolve(req.file));
      const result = await renderHwpxToSvg(toArrayBuffer(buffer), {
        highlights: req.highlight,
        reflow: req.reflow,
        reflowMode: req.reflowMode
      });
      const outPath = resolve(req.out);
      mkdirSync(dirname(outPath), { recursive: true });
      writeFileSync(outPath, result.svg, "utf-8");
      write({ id, ok: true, out: outPath, width: result.width, height: result.height, pageCount: result.pageCount, stats: result.stats, warnings: result.warnings });
    } catch (err) {
      write({ id, ok: false, error: sanitizeError(err) });
    }
  }
});
program.command("mcp").description("MCP \uC11C\uBC84 \uC2E4\uD589 (Claude / Cursor / Windsurf \uC5F0\uB3D9)").action(async () => {
  const { startMcpServer } = await import("./mcp.js");
  await startMcpServer();
});
program.command("setup").description("\uB300\uD654\uD615 \uC124\uCE58 \uB9C8\uBC95\uC0AC \u2014 AI \uD074\uB77C\uC774\uC5B8\uD2B8 \uC790\uB3D9 \uB4F1\uB85D (Mac/Win/Linux)").action(async () => {
  const { runSetup } = await import("./setup-MG3YYKXU.js");
  await runSetup();
});
program.command("check-formula-models").description("PDF \uC218\uC2DD OCR \uBAA8\uB378(MFD + MFR + tokenizer, ~155MB) \uC0C1\uD0DC \uD655\uC778 \u2014 \uC5C6\uAC70\uB098 SHA \uBD88\uC77C\uCE58\uBA74 \uB2E4\uC6B4\uB85C\uB4DC").option("--status-only", "\uC0C1\uD0DC\uB9CC JSON \uC73C\uB85C \uCD9C\uB825 (\uB2E4\uC6B4\uB85C\uB4DC \uC548 \uD568)").action(async (opts) => {
  try {
    const { getFormulaModelStatus, ensureFormulaModels, getFormulaModelsDir } = await import("./formula-IFKZZPD2.js");
    const dir = getFormulaModelsDir();
    if (opts.statusOnly) {
      const status = await getFormulaModelStatus();
      process.stdout.write(
        JSON.stringify(
          {
            modelsDir: dir,
            allReady: status.every((s) => s.verified),
            models: status.map((s) => ({
              name: s.spec.name,
              filename: s.spec.filename,
              sizeMb: s.spec.sizeMb,
              exists: s.exists,
              verified: s.verified,
              invalidReason: s.invalidReason,
              path: s.localPath
            }))
          },
          null,
          2
        ) + "\n"
      );
      return;
    }
    process.stderr.write(`[kordoc-formula] \uCE90\uC2DC \uB514\uB809\uD1A0\uB9AC: ${dir}
`);
    await ensureFormulaModels((p) => {
      if (p.phase === "download" && p.total) {
        const pct = Math.floor(p.downloaded / p.total * 100);
        process.stderr.write(
          `\r[kordoc-formula] ${p.spec.name} ${pct}% (${(p.downloaded / 1024 / 1024).toFixed(1)}/${(p.total / 1024 / 1024).toFixed(1)}MB)`
        );
        if (p.downloaded >= p.total) process.stderr.write("\n");
      } else if (p.phase === "verify") {
        process.stderr.write(`[kordoc-formula] ${p.spec.name} SHA-256 \uAC80\uC99D \uC911...
`);
      } else if (p.phase === "done") {
        process.stderr.write(`[kordoc-formula] ${p.spec.name} \uC900\uBE44 \uC644\uB8CC
`);
      } else if (p.phase === "skip") {
        process.stderr.write(`[kordoc-formula] ${p.spec.name} \uC774\uBBF8 \uC874\uC7AC (skip)
`);
      }
    });
    process.stdout.write("ok\n");
  } catch (err) {
    process.stderr.write(`[kordoc] \uC218\uC2DD \uBAA8\uB378 \uC900\uBE44 \uC2E4\uD328: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("check-ocr-models").description("\uD14D\uC2A4\uD2B8 OCR \uBAA8\uB378(PP-OCRv5 korean det+rec, ~18MB) \uC0C1\uD0DC \uD655\uC778 \u2014 \uC5C6\uAC70\uB098 SHA \uBD88\uC77C\uCE58\uBA74 \uB2E4\uC6B4\uB85C\uB4DC").option("--status-only", "\uC0C1\uD0DC\uB9CC JSON \uC73C\uB85C \uCD9C\uB825 (\uB2E4\uC6B4\uB85C\uB4DC \uC548 \uD568)").action(async (opts) => {
  try {
    const { getOcrModelStatus, ensureOcrModels, getOcrModelsDir } = await import("./models-QQA7OQHY.js");
    const dir = getOcrModelsDir();
    if (opts.statusOnly) {
      const status = await getOcrModelStatus();
      process.stdout.write(
        JSON.stringify(
          {
            modelsDir: dir,
            allReady: status.every((s) => s.verified),
            models: status.map((s) => ({
              name: s.spec.name,
              filename: s.spec.filename,
              sizeMb: s.spec.sizeMb,
              exists: s.exists,
              verified: s.verified,
              invalidReason: s.invalidReason,
              path: s.localPath
            }))
          },
          null,
          2
        ) + "\n"
      );
      return;
    }
    process.stderr.write(`[kordoc-ocr] \uCE90\uC2DC \uB514\uB809\uD1A0\uB9AC: ${dir}
`);
    await ensureOcrModels((p) => {
      if (p.phase === "download" && p.total) {
        const pct = Math.floor(p.downloaded / p.total * 100);
        process.stderr.write(
          `\r[kordoc-ocr] ${p.spec.name} ${pct}% (${(p.downloaded / 1024 / 1024).toFixed(1)}/${(p.total / 1024 / 1024).toFixed(1)}MB)`
        );
        if (p.downloaded >= p.total) process.stderr.write("\n");
      } else if (p.phase === "verify") {
        process.stderr.write(`[kordoc-ocr] ${p.spec.name} SHA-256 \uAC80\uC99D \uC911...
`);
      } else if (p.phase === "done") {
        process.stderr.write(`[kordoc-ocr] ${p.spec.name} \uC900\uBE44 \uC644\uB8CC
`);
      } else if (p.phase === "skip") {
        process.stderr.write(`[kordoc-ocr] ${p.spec.name} \uC774\uBBF8 \uC874\uC7AC (skip)
`);
      }
    });
    process.stdout.write("ok\n");
  } catch (err) {
    process.stderr.write(`[kordoc] OCR \uBAA8\uB378 \uC900\uBE44 \uC2E4\uD328: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.command("models").description("OCR/\uC218\uC2DD \uBAA8\uB378 \uC624\uD504\uB77C\uC778 \uC0AC\uC774\uB4DC\uB85C\uB4DC \u2014 \uD3D0\uC1C4\uB9DD \uBC18\uC785\uC6A9 \uB0B4\uBCF4\uB0B4\uAE30/\uAC00\uC838\uC624\uAE30 (SHA-256 \uAC80\uC99D)").option("--status", "\uCE90\uC2DC\uC5D0 \uC124\uCE58\uB41C \uBAA8\uB378 \uC0C1\uD0DC\uB97C JSON \uC73C\uB85C \uCD9C\uB825").option("--export <dir>", "\uB85C\uCEEC \uCE90\uC2DC \u2192 \uBC88\uB4E4 \uB514\uB809\uD1A0\uB9AC\uB85C \uB0B4\uBCF4\uB0B4\uAE30 (\uC628\uB77C\uC778 PC\uC5D0\uC11C \uC2E4\uD589)").option("--import <dir>", "\uBC88\uB4E4 \uB514\uB809\uD1A0\uB9AC \u2192 \uB85C\uCEEC \uCE90\uC2DC\uB85C \uC124\uCE58 (\uB0B4\uBD80\uB9DD PC\uC5D0\uC11C \uC2E4\uD589)").action(async (opts) => {
  try {
    const { exportModels, importModels, modelCacheStatus } = await import("./model-bundle-Q4HQXJQG.js");
    if (opts.status) {
      process.stdout.write(JSON.stringify(await modelCacheStatus(), null, 2) + "\n");
      return;
    }
    const dir = opts.export ?? opts.import;
    if (!dir) {
      process.stderr.write("[kordoc] --status / --export <dir> / --import <dir> \uC911 \uD558\uB098\uAC00 \uD544\uC694\uD569\uB2C8\uB2E4\n");
      process.exit(1);
    }
    if (opts.export && opts.import) {
      process.stderr.write("[kordoc] --export \uC640 --import \uB294 \uD568\uAED8 \uC4F8 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4\n");
      process.exit(1);
    }
    const result = opts.export ? await exportModels(dir) : await importModels(dir);
    for (const f of result.files) {
      const mark = f.status === "copied" ? "+" : f.status === "invalid" ? "!" : "-";
      process.stderr.write(`  ${mark} ${f.group}/${f.filename}${f.reason ? ` \u2014 ${f.reason}` : ""}
`);
    }
    const verb = opts.export ? "\uB0B4\uBCF4\uB0B4\uAE30" : "\uC124\uCE58";
    const copied = result.files.filter((f) => f.status === "copied").length;
    process.stderr.write(
      `[kordoc-models] ${verb} ${result.ok ? `\uC644\uB8CC (${copied}\uAC1C)` : "\uC2E4\uD328 \u2014 \uC704 ! / - \uD56D\uBAA9 \uD655\uC778"}: ${dir}
`
    );
    if (!result.ok) process.exit(1);
    process.stdout.write("ok\n");
  } catch (err) {
    process.stderr.write(`[kordoc] \uBAA8\uB378 \uC0AC\uC774\uB4DC\uB85C\uB4DC \uC2E4\uD328: ${sanitizeError(err)}
`);
    process.exit(1);
  }
});
program.parse();
//# sourceMappingURL=cli.js.map